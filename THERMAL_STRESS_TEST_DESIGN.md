# 高低温环境压力测试开发设计

## 1. 目标与范围

本文定义 SpaceTestPC 上位机与 PCBA `spacetest3576` 的高低温环境压力测试方案。

测试在温箱中进行，温箱不接入上位机：操作员人工设定温度、保温，并在上位机确认阶段开始。上位机负责运行自动化测试、持续记录、断线恢复和最终汇总。

默认目标时长为 **72 小时**。设计重点是长期稳定性、异常定位和结果可追溯，而非单次功能验证。

### 1.1 纳入自动化压力测试的项目

- `board_state`：PCBA 服务、SN、板卡状态与会话健康检查。
- `wifi`：目标 SSID 扫描、RSSI、重试和网络连通性。
- `ethernet`：链路、网关 Ping、重连与丢包。
- `bluetooth`：目标蓝牙广播扫描及 RSSI。
- `emmc_ddr`：受限目录中的 eMMC 读写校验、DDR 压力和错误检测。
- `tf`：TF 卡挂载、读写、校验与掉盘检测。
- `usb2_3`：USB 枚举、挂载和读写校验。
- `typec_camera`：已接入测试夹具时的视频流连续性。
- `fan`：PWM 控制、转速采样与恢复。
- `pcba_test_points`：已有自动采样接口时的测试点采集。

### 1.2 不纳入本方案的项目

- 电压、电流测试：高低温温箱场景不具备稳定的外部仪表接入条件。
- 按键测试：不能在无人值守循环中可靠执行。

HDMI、LCD、指示灯和网口灯可保留在每个温度阶段的开始/结束做人工或夹具确认，但不进入高频自动循环。

## 2. 测试原则

1. **温度人工控制，测试自动运行。** 上位机不推断实际温度，只记录操作员填写的目标温度及确认时间。
2. **任务按频率分层。** 网络健康检查高频执行；存储、CPU 和内存压力中频执行；完整功能复测低频执行。
3. **存储写入受控。** 所有写入只能发生在专用测试目录；记录累计写入量；不得直接写块设备。
4. **单轮失败不立即结束。** 可恢复的网络或扫描异常记录后继续；存储校验错误、系统失联等关键错误立即判定阶段失败。
5. **上位机是唯一的长期记录主体。** 即使 PCBA 重启、服务退出或设备时间漂移，上位机仍保留完整事件时间线。

## 3. 三天测试计划

温度值需按产品和器件规格确认。下表仅为默认示例，不是强制规格。

| 阶段 | 建议温度 | 保温 | 压力时长 | 目的 |
|---|---:|---:|---:|---|
| 常温基线 | 25℃ | 10 分钟 | 30 分钟 | 确认初始功能和网络基线 |
| 高温 | 60℃ | 30 分钟 | 24 小时 | 高温运行、存储与网络稳定性 |
| 温度转换 | 人工降温 | 到温后确认 | 不计入有效压力时长 | 记录温变过程，不执行高负载任务 |
| 低温 | -20℃ | 30 分钟 | 24 小时 | 冷态网络、存储和服务稳定性 |
| 温度转换 | 人工升温 | 到温后确认 | 不计入有效压力时长 | 记录温变过程，不执行高负载任务 |
| 恢复常温 | 25℃ | 30 分钟 | 约 23 小时 | 长时间恢复验证及最终复测 |

每个阶段由操作员执行以下动作：

1. 人工设置温箱目标温度。
2. 在上位机填写目标温度、操作员和设温时间。
3. 温箱到温且保温完成后，点击“温度已稳定，开始压力测试”。
4. 上位机运行阶段基线检查和自动循环。
5. 阶段结束后执行一次完整功能复测，确认阶段结论。

## 4. 自动化循环策略

### 4.1 高频健康循环（每 5 分钟）

- `board_state`
- Wi-Fi 扫描和 RSSI 判定
- Wi-Fi 到指定网关或服务器的 Ping
- Ethernet 链路和 Ping
- Bluetooth 广播扫描
- 风扇状态或转速读取
- PCBA 服务心跳

目的：尽快发现短时掉线、无线扫描异常、服务退出和风扇失效。

### 4.2 中频负载循环（每 30 分钟）

- CPU 压力：50%～80% 负载、限定时长。
- 内存压力：使用总内存的 50%～60%，禁止触发 OOM。
- eMMC：128～256 MiB 测试文件顺序/随机读写、MD5 或 CRC 校验。
- TF：挂载、读写、校验和状态检查。
- USB：已接入夹具或 U 盘时执行枚举、读写、校验。
- Type-C 摄像头：已接入夹具时进行短时连续流检测。

### 4.3 功能检查点（每 4 小时）

执行当前自动化测试项的完整一次组合，并保存阶段性汇总。人工项目仅在操作员在场并明确发起时运行。

### 4.4 重启恢复检查（每 12 或 24 小时）

可配置执行受控的服务重启或整机冷重启。设备重新上线后必须完成：

1. PCBA 服务连接；
2. `board_state`；
3. Wi-Fi、Ethernet、存储快速检查；
4. 恢复原压力测试阶段和循环编号。

若产品规范不允许重启，此项目必须禁用并在报告中标明。

## 5. 存储压力保护

eMMC 与 TF 压力测试必须遵循：

- 目录固定为 `/userdata/factory_test/stress`，不允许由操作员输入路径。
- 单轮最大写入 256 MiB；建议轮换 4～8 个测试文件。
- 每次写入后必须读回校验。
- 默认每 30 分钟一次，72 小时累计写入约 36 GiB。
- 测试结束后保留失败现场文件，成功时按配置清理临时文件。
- 任一校验失败、挂载异常或 I/O error 均属于关键失败。

## 6. Wi-Fi 与 Ethernet 判定

### 6.1 Wi-Fi

Wi-Fi 需要配置固定的测试 SSID，优先使用独立的 2.4 GHz 测试网络。温箱屏蔽影响较大时，应使用外接天线或射频馈线，并在常温阶段记录 RSSI 基线。

每轮记录：

- SSID 是否发现；
- RSSI；
- 扫描和重连次数；
- Ping 成功率与丢包率；
- 连续失败次数。

建议默认 `minRssi` 为 `-70 dBm`，最终阈值应根据温箱、天线和常温基线校准。

### 6.2 Ethernet

每轮记录链路状态、IP、网关 Ping、平均延迟、丢包率和链路恢复时间。温箱穿线孔使用的网线应固定，应避免在无人值守循环内要求人工插拔。

## 7. 状态机与故障恢复

上位机压力测试状态：

```text
created
waiting_temperature_confirmation
baseline_check
running
recovering_connection
paused
stage_failed
stage_completed
completed
aborted
```

### 7.1 断线恢复

- PCBA 会话中断时，上位机记录最后测试项、时间和循环号。
- 每 5 秒尝试重连，连续 10 分钟失败则标记“设备失联”。
- 重连成功后执行 `board_state` 和快速健康检查。
- 若恢复成功，继续当前循环的下一安全任务；不得将未完成任务标记为通过。

### 7.2 失败分级

| 级别 | 示例 | 动作 |
|---|---|---|
| 瞬时失败 | 单次 Wi-Fi 未扫描到 SSID、单次 Ping 丢包 | 记录，进入下一轮 |
| 连续失败 | 同项目连续 3 轮失败 | 阶段标记失败，继续收集或由操作员停止 |
| 关键失败 | eMMC/TF 校验失败、系统重启、服务长时间失联 | 立即标记阶段失败并保存现场 |
| 安全失败 | 温度/风扇安全策略触发（若具备传感器） | 立即停止负载并提示人工处理 |

## 8. 配置模型

建议在 `appsettings.json` 增加：

```json
{
  "thermalStress": {
    "enabled": true,
    "totalDurationHours": 72,
    "healthIntervalMinutes": 5,
    "loadIntervalMinutes": 30,
    "fullCheckIntervalHours": 4,
    "rebootCheckIntervalHours": 24,
    "reconnectIntervalSeconds": 5,
    "reconnectTimeoutMinutes": 10,
    "wifi": {
      "ssid": "Xiaomi_9994",
      "minRssi": -70,
      "maxConsecutiveFailures": 3
    },
    "storage": {
      "directory": "/userdata/factory_test/stress",
      "fileSizeMiB": 256,
      "fileCount": 4,
      "maxTotalWriteGiB": 40
    },
    "stages": [
      { "name": "baseline", "targetTemperatureC": 25, "soakMinutes": 10, "durationMinutes": 30 },
      { "name": "high", "targetTemperatureC": 60, "soakMinutes": 30, "durationMinutes": 1440 },
      { "name": "low", "targetTemperatureC": -20, "soakMinutes": 30, "durationMinutes": 1440 },
      { "name": "recovery", "targetTemperatureC": 25, "soakMinutes": 30, "durationMinutes": 1380 }
    ]
  }
}
```

温度阶段的开始时间应以操作员确认时间为准，不以设温时间为准。

## 9. 数据记录与报告

### 9.1 原始事件

按天或按文件大小轮转保存 `stress-detail-YYYYMMDD.jsonl`。每条事件必须包含：

- SN、Board ID、会话 ID；
- 阶段名称、目标温度、操作员；
- 上位机 UTC 时间与阶段 `elapsedSec`；
- 循环编号、测试项、原始结果和判定；
- 失败原因、恢复状态和关联日志文件。

### 9.2 汇总文件

测试完成时自动生成：

- `stress-summary.csv`：供 Excel 统计；
- `stress-report.md`：供研发评审与归档；
- 可选 PDF 报告。

报告至少包括：

- 总时长、有效压力时长、阶段实际时长；
- 每阶段计划/完成循环数；
- 各项目执行次数、通过率、失败次数和连续失败次数；
- 首次失败时间、最后失败时间；
- Wi-Fi 最低 RSSI、扫描成功率、丢包率；
- Ethernet 丢包率和最长恢复时间；
- eMMC/TF/USB 校验错误数与累计写入量；
- PCBA 服务掉线次数和最长失联时间；
- 常温恢复后的最终复测结论。

## 10. 验收标准

环境压力测试通过必须同时满足：

1. 所有阶段按计划完成，或中断原因已明确记录；
2. 无 eMMC、TF、USB 数据校验错误；
3. 无未恢复的系统/PCBA 服务失联；
4. Wi-Fi、Ethernet 未出现超过阈值的连续失败；
5. 受控重启后设备可恢复并通过快速健康检查；
6. 恢复常温后完整自动化功能复测通过；
7. 结果文件、阶段记录和失败现场完整可追溯。

任一关键失败或任一阶段最终失败，72 小时环境压力测试结论为 FAIL。

## 11. 实施顺序

1. 新增上位机 `thermal_stress` 模式、阶段确认 UI 和 72 小时计时器。
2. 复用现有 PCBA 自动测试接口，增加循环调度和测试项分组。
3. 增加 CPU/内存受限压力命令和 eMMC 写入量统计。
4. 实现断线重连、断点恢复和失败快照。
5. 实现 JSONL、CSV、Markdown 报告输出与日志轮转。
6. 用 30 分钟常温试运行验证，再执行完整 72 小时方案。
