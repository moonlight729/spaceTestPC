# 老化测试总体设计方案

> **状态：规划中，未实施（截至 2026-09）。**
>
> **重建说明**：本文件曾误删，且从未纳入 git，无法从版本控制恢复。当前版本依据方案沟通过程中的
> 记录重建，并对照 [`architecture.md`](docs/architecture.md) 与
> [`communication-protocol.md`](docs/communication-protocol.md) 校准了协议、端口与工程结构。
> 已确认结论（§16）与待标定项（§17）中，凡非原样保留的条目均按方案上下文补全，**评审时请以本文件
> 为准逐条确认**，确认后请删除本说明块。

## 1. 定位与边界

当前已有两套测试形态，老化是第三套，不能与前两者混用同一份判定口径：

| 形态 | 目的 | 时长 | 负载 | 判定主体 | 网络 |
|---|---|---|---|---|---|
| PCBA 测试 | 板级接口/电压/连通性 | 分钟级 | 无 | 上位机 + 仪器 | 1 对 1，电脑与设备同路由器 |
| 整机测试 | 装配后的功能确认 | 分钟级 | 无 | 人工 + 板端 | 1 对 1，电脑与设备同路由器 |
| **老化测试（本文）** | 模拟真实出货场景的常温长跑 | 8h～72h 可配 | **真实业务负载** | **设备端自治** | **1 对 32，独立专网** |

老化的核心特征是"跑真实业务"而不是"跑压力工具"：摄像头真实拉流、真实编码写盘、真实网口传文件、
真实语音播报，各项按真实业务节奏并行推进；而不是把 CPU 打满、把带宽压满。因此判定关注的也是
"长时间持续不中断、数据不损坏、指标不漂移"，而不是"能跑多快"。

由此带来三条与其他形态根本不同的约束：

1. **判定主体在设备端。** 8h～72h 内上位机可能重启、断网、被关闭，判定不能依赖它在线。
2. **负载是业务不是工具。** 不许引入 `stress` 类合成负载工具，那测不出真实场景的问题。
3. **规模是 1 对 32。** 单机 1 对 1 的会话模型（扫码 → 下发计划 → 逐项等待）在 32 台上不成立。

## 2. 总体架构

### 2.1 网络拓扑

老化使用**独立专网**，与产线办公网、与现有 PCBA 测试网分开：

```text
[老化 PC · 静态 IP 192.168.110.2] ── [32 口交换机] ── [设备 .101 ~ .132]
```

- 设备 IP 由上级路由器按 MAC 静态绑定，只在路由器改绑定，不改设备配置。
- **老化 PC 必须使用静态 IP**：设备要反向连接它的 19002 端口传文件，地址由
  `aging.start` 下发，PC 地址不固定会导致网传项失效。
- **DHCP 地址池与静态段错开**，避免地址冲突。
- **SN 校验防呆**：按 MAC 绑定后，若更换设备（新 MAC）拿不到工位表里的 IP。因此连上
  后必须读一次 SN 与工单比对，不匹配则告警并跳过，防止误启动非目标设备。

### 2.2 分层

```text
┌─ 管控层（独立 PC · 静态 IP · 全程可断）─────────────┐
│  SpaceTestPC.AgingStation                          │
│   ├ SlotTable            工位表 slot → IP，不扫描    │
│   ├ UpgradeOrchestrator  分批升级 / 单台隔离回滚     │
│   ├ JobDispatcher        aging.start 一次下发        │
│   ├ NetSinkService:19002 边收边丢 + MD5 校验         │
│   └ ResultCollector      回收 result + 出报告        │
└────────────────────────────────────────────────────┘
                    │ 19001 控制面（上位机 → 设备）
                    │ 19002 数据面（设备 → 上位机，反向连）
┌─ 设备层（每台 · 自治 · 上位机断开照跑）──────────────┐
│  spacetest3576（同一个二进制 OTA）                   │
│   ├ [既有] pcba / finished_product  ← 一行不改       │
│   ├ [新增] aging 命令组（dispatch 末尾追加）         │
│   ├ AgingEngine   懒启动，结束即销毁                 │
│   ├ state.json ←→ LVGL 显示（可选，enableLvgl 开关） │
│   └ 落盘  job / state / events / result             │
└────────────────────────────────────────────────────┘
```

### 2.3 端口与通信方向

| 端口 | 面 | 方向 | 用途 |
|---|---|---|---|
| `19001` | 控制面 | 上位机 → 设备 | `aging.start` / `stop` / `status` / `result`，复用既有命令路由与 19001 端口 |
| `19002` | 数据面 | 设备 → 上位机 | NetSink 文件传输，**设备主动反向连接**，地址由 `aging.start` 下发 |
| — | 事件 | 设备 → 上位机 | 复用 `test.report` 通道推 `aging.progress` |

控制面是上位机主动连设备（1 对 32 全连）；数据面是设备反向连老化 PC（故 PC 必须静态 IP）。
两个方向不同，这是静态 IP 成为硬要求的唯一原因。

## 3. 与现有系统的兼容（硬要求）

老化能力随同一个 `spacetest3576` 二进制 OTA 下发。加入老化机制后，同一台设备拿去做
PCBA 或整机测试，必须和今天完全一致。

### 3.1 板端三条硬要求

| # | 要求 | 具体做法 |
|---|---|---|
| 1 | **零副作用** | 未收到 `aging.start` 时，老化相关线程**根本不存在**。不允许常驻后台线程轮询——那会改变 CPU/内存/功耗画像，PCBA 测试环境就变了 |
| 2 | **既有分支不动** | `aging` 命令组追加在命令 dispatch 链**末尾**；既有测试项、错误码、事件格式一行不改 |
| 3 | **未知命令不崩** | 老上位机连新板端、新上位机连老板端，都要优雅返回 `unsupported`，不得断连或崩溃 |

实现形态：`AgingEngine` **懒启动**——`aging.start` 到达才创建，结束或 `stop` 后销毁。

### 3.2 上位机模式隔离

- 既有 `SpaceTestPC.App`（PCBA / 整机）**一行不改**，不新增 `aging` 模式。
- 协议模型、命令客户端、配置加载、SQLite 落库、CSV 导出抽到共享的 `SpaceTestPC.Core`，
  老化站与既有上位机共用，**不重复实现**。
- `appsettings.json` 与数据库各自独立，避免老化长跑数据混进 PCBA 库。
- 判据、报告、UI 全部独立，不与 PCBA / 整机共用判定口径。

### 3.3 回归验证清单

1. 未下发 `aging.start` 时，进程列表中看不到老化相关线程，CPU / 内存 / 功耗与今天一致。
2. PCBA 全项回归：结论、错误码、事件格式逐项对比，无差异。
3. 整机全项回归：同上。
4. 老上位机 + 新板端：下发未知命令返回 `unsupported`，不断连、不崩。
5. 新上位机 + 老板端：`aging.start` 返回 `unsupported`，UI 提示"板端不支持老化"，不影响其它功能。
6. 升级失败回滚后，设备仍能正常执行 PCBA 测试。

## 4. 上位机形态：共享核心 + 独立应用

### 4.1 选型

不做成"给现有上位机加一个模式"，而是**共享核心 + 独立应用**：

- 老化是 32 路长连接 + 8h～72h 长跑 + 独立专网，与 1 对 1 的分钟级会话 UI 混在一个进程里
  会互相拖累（UI 刷新、数据库连接、升级检查都会被长跑绑住）。
- 但协议、落库、配置这些**不能重写第二份**，否则两边必然漂移。

```text
SpaceTestPC.Core（从现有 App 抽出，共享）
  ├ 协议模型 / 命令客户端（含多实例改造）
  ├ 配置加载 / SQLite 落库 / CSV 导出
  └ 日志

SpaceTestPC.AgingStation（新独立 exe，装到老化 PC）
  ├ SlotTable / AgingStationManager
  ├ UpgradeOrchestrator（分批 + 单台隔离回滚）
  ├ JobDispatcher
  ├ NetSinkService:19002
  ├ ResultCollector
  └ 槽位网格 UI + 详情 + 报告
```

### 4.2 工程结构

```text
SpaceTestPC.AgingStation/
  ├ Services/
  │   SlotTable              工位表：slot → IP / 期望 SN，不扫描
  │   AgingStationManager    32 路生命周期总控
  │   UpgradeOrchestrator    分批升级 + 单台隔离回滚
  │   JobDispatcher          aging.start 下发（含统一编排的开始时刻）
  │   NetSinkService         19002 收流服务
  │   ResultCollector        回收 result.json + 出报告
  └ ViewModels/Views/
      SlotGridViewModel      32 槽位网格（绿/黄/红）
      SlotDetailViewModel    单台详情、失败原因
```

### 4.3 1:N 改造点（现状差距）

现有客户端是单例、单连接，`MainViewModel` 假设"当前只有一台设备"。改 1:N 需处理：

1. 客户端**多实例化**，支持按 host/port 创建独立连接，DI 生命周期由单例改为工厂/瞬态
2. **每槽位独立生命周期**：独立 client + 独立 `CancellationToken` + 独立重连退避
3. **报告单写入者**：32 路并发写 SQLite 必然锁竞争，用队列串行化
4. **UI 节流**：32 个槽位实时刷新会拖死 UI 线程
5. 确认底层是**真异步 IO**；若为同步阻塞读，32 路会吃掉 32 个线程

### 4.4 路数策略

连接数与操作并发**分开**：

| 类型 | 并发 | 理由 |
|---|---|---|
| 监控连接 | 32 路全连 | 一条 TCP 很便宜，事件每 60s 一条，带宽可忽略 |
| 网口传文件 | **6~8 台** | 千兆下 32 台同时打流必然拥塞，会集体假失败 |
| 批量升级 | **8 台一批** | 二进制经 19001 上传，耗时长，需分批 |

按 32 口交换机设计，槽位数可配置；扩容到 40 台只需加工位表。

## 5. 设备端自治模型

### 5.1 命令组 `aging`

```json
{
  "protocolVersion": "1.0",
  "sessionId": "aging-slot03-20260920",
  "sn": "S1ABCD10012",
  "commandGroup": "aging",
  "command": "start",
  "parameters": {
    "runId": "run-20260920-0015",
    "startDelaySec": 47,
    "durationSec": 28800,
    "reportIntervalMs": 60000,
    "enableLvgl": true,
    "netSinkHost": "192.168.110.2",
    "netSinkPort": 19002,
    "items": [
      { "id": "camera_record_emmc", "parameters": { } },
      { "id": "cpu_load",  "parameters": { "targetPercent": 30 } },
      { "id": "mem_load",  "parameters": { "targetPercent": 30 } }
    ]
  }
}
```

- 板端**立即返回** `accepted`，后台起任务，主线程继续应答
- `startDelaySec > 0` 时先进入 `PREPARING` 倒计时，归零后才开始计有效时长（见 5.5）
- 按 `reportIntervalMs` 推送 `aging.progress`（复用 `test.report` 通道）
- `stop` / `status` / `result` 三个命令补齐生命周期
- `netSinkHost` 由上位机下发，**不得硬编码在板端**

### 5.2 文件契约与目录隔离

老化产生的文件分两类，**必须分开存放**——这是"能删干净"的前提：

```text
/userdata/factory_test/aging/          元数据区（KB 级，保留至回收）
  job.json      下发即落盘（入参、SN、时长、各项参数）
  state.json    周期 fsync（当前项、elapsedSec、累计指标、resumeCount）
  events.jsonl  明细
  result.json   结束结论（设备自评，唯一真相源）
  media/                               大数据区（GB 级，测完整体删除）
    rec/        录像分段（camera_record_emmc）
    tmp/        其它临时产物
```

| 规则 | 说明 |
|---|---|
| 大数据只准落在 `media/` | 录像、临时文件一律写到 `media/` 下，**禁止**写到 `aging/` 根或任意其它路径 |
| 路径固定，不接受外部传入 | `media/` 由板端常量定义，`aging.start` **不带**路径参数，防止误删系统目录 |
| 清理 = 删 `media/` 一个目录 | 不做逐文件匹配删除，避免漏删与误删 |
| 元数据保留至回收 | 上位机用 `aging.result` 收走后才可清，或由下一轮 `aging.start` 覆盖 |

一句话：**一个目录进，一个目录出。** 目录边界划清楚，清理才有确定性。

写文件采用"先写 `.tmp` 再 rename"并 `fsync`，保证掉电安全。

### 5.3 状态机

```text
IDLE → PREPARING（倒计时）→ RUNNING → CLEANING → WAITING_MANUAL_CHECK → PASSED / FAILED
                                │                        ↑
                                └─ 关键失败 ─────────────┘（保留现场，跳过清理）
```

`PREPARING` 只做倒计时与启动前自检，**不计有效时长**。倒计时期间掉电，重启后按 5.4
resume，继续倒计时。

`CLEANING` 只走**正常达标**路径：删 `media/` 并校验（见 8.9）。**关键失败时跳过 `CLEANING`**
——此时录像就是证据，删掉等于毁掉现场，由人工决定何时清理。

`WAITING_MANUAL_CHECK` 既是终检态，也是关键失败后的**冻结态与处置入口**：人工取证 →
清理或复测 → 设备回 `IDLE`（流程见 12.1）。

**关键设计**：老化跑完不直接结束，停在 `WAITING_MANUAL_CHECK`。人工终检结果无论来自
设备屏还是上位机，都写回 `result.json` 的同一字段。否则无屏设备的终检结果无处安放。

### 5.4 断线与掉电语义

| 场景 | 处理 |
|---|---|
| 上位机断开 | **不影响**，设备继续跑完；上位机恢复后用 `status` / `result` 拉回 |
| 掉电/重启 | 启动时发现未完成 job → 自动 resume，记 `resumeCount` / `lostTimeSec` |
| 续跑上限 | `maxResumeCount = 2`，超限记异常，需人工判断 |
| 有效时长 | 按**累计有效运行时长**判定，不是墙钟时间 |

**严禁**把"上位机断开"设计成"老化中止"，否则一次上位机重启会作废 32 台的长跑。

### 5.5 启动触发与批量编排

上位机提供三种启动粒度：

| 粒度 | 入口 | 行为 |
|---|---|---|
| 单槽位 | 槽位卡片上的"开始" | 只给该台下发 `aging.start` |
| 选中槽位 | 勾选若干 → "开始选中" | 按序号错开，只下发选中的 |
| 全部 | 顶部"全部开始" | 按序号错开，下发所有在线且就绪的槽位 |

#### 批量启动：统一编排 + 依次错开

上位机下发是 8 路并发分批进行的，各台收到命令的时刻天然有先后。若让设备"收到即开跑"，
起始时刻就不可控。所以批量启动统一由上位机**编排开始时刻**，并**有意依次错开**：

```text
1. 点击开始 → 定基准 baseStartAtUtc = now + batchPrepareSec（默认 60）
2. 参与本批的槽位按序号排序，第 k 台（k 从 0 起）：
      startAtUtc(k) = baseStartAtUtc + k × batchStaggerSec     // 默认 30s
3. 下发第 k 台时单独计算 startDelaySec = startAtUtc(k) − 下发时刻
4. 设备收到 → 校验 → 落 job.json → PREPARING → 本地倒计时归零 → RUNNING
   从这一刻起计有效时长
5. 倒计时在设备本地用相对秒数跑，不依赖上位机在线，也不依赖设备间时钟同步
```

32 台 × 30 s 即首尾相差约 15.5 min，最后一台的结束时刻相应后延。相对 8 h~72 h 的
老化时长，这个偏移在 3% 以内，可接受；配套要求是**报告必须记录每台的实际开始时刻**
（`startedAtUtc`），否则无法解释同批次的时间差。

**错开带来三个额外收益**（这正是选择错开而非齐开的原因）：

1. 避免 32 台同时开始拉流 + 编码写盘造成的瞬时 I/O 与功耗冲击
2. `ethernet_transfer` 每 30 min 触发一次，齐开会让 32 台撞在同一时刻；错开后自然错峰，
   与 §8.3 的 8 路并发限流叠加，网传更稳
3. 删除水位、清理等由各台进度驱动的离散事件也随之分散

UI 上批次启动期间显示：`已下发 12/32，首台 12:30:00，间隔 30s`；槽位卡片显示该台自己的
`预计开始 12:40:30`。

**这个设计不破坏设备端自治**：上位机下发完即可关闭，设备自己到点开跑。
反之，若做成"上位机到点广播 commit"，老化就重新被绑回上位机在线，违背 §2。

#### 启动失败的槽位处理

| 情况 | 处理 |
|---|---|
| 算出 `startDelaySec <= 0`（该台下发过晚或重试超时） | 该台**不参与本批**，UI 标"未赶上批次"，可用新的基准时刻单独补发 |
| 下发返回 busy / error（升级中、上轮未清理） | 标记该槽位失败，**不阻塞其它槽位** |
| 离线槽位 | 跳过，标"离线未启动" |
| 重复下发 | `aging.start` 带 `runId` 幂等：已有相同 `runId` 的 job 时返回当前状态，不重启、不重复计时 |

单槽位启动用较小的 `singlePrepareSec`（默认 10 s）且**不加 stagger**，没必要等批次窗口。

置 `batchStaggerSec = 0` 即退化为全部齐开，保留这个开关以便需要严格对齐时切换。

#### 停止与补跑

- `aging.stop` 支持单台与全部；**"全部停止"是维护操作，需二次确认**
- 批次中途上电/恢复的设备走 5.4 的 resume，其结束时刻会晚于本批，报告按
  `resumeCount` / `lostTimeSec` 标注，不影响其它槽位
- 各台达到有效时长后各自进入 `WAITING_MANUAL_CHECK`，**不追求统一结束**
- **批次整体完成时间 = 最后一台的开始时刻 + 有效时长**，取槽位中的最大值，而非第一台

## 6. 结果显示：三层模型

```text
┌─ 汇总层（必做）────────┐  ┌─ 现场层（推荐）────┐
│  AgingStation 上位机    │  │  LVGL 设备屏        │
│  32 台槽位网格 绿/黄/红 │  │  极简状态+终检引导  │
│  详情/失败原因/报告/DB  │  │  只看关键几个数     │
└────────────────────────┘  └────────────────────┘
```

### 6.1 三条设计原则

1. **判定在设备端，`result.json` 是唯一真相源。** 上位机与设备屏都是读者，不得各自下结论，
   否则两处结论不一致时无从仲裁。
2. **汇总看上位机，现场看设备屏。** 上位机回答"这一批怎么样、哪台要处理"；
   设备屏回答"这台现在在干什么、下一步要我做什么"。两者信息量刻意不同。
3. **现场层不承载判定逻辑。** 设备屏只读 `state.json`，它崩溃、不存在、被 kill 都不影响老化跑完。

## 7. 测试项清单

| # | id | 显示名 | 节奏 | 判定要点 |
|---|---|---|---|---|
| 1 | `camera_record_emmc` | 摄像头录存 + 水位删除 | 持续 | 录像不中断、CRC 无误、删除与清理正常 |
| 2 | `cpu_load` | CPU 30% 占用 | 持续 | 水位闭环稳定、进程不退出 |
| 3 | `mem_load` | 内存 30% 占用 | 持续 | 水位闭环稳定、不 OOM |
| 4 | `ethernet_transfer` | 网口文件传输 | 每 30 min | 完成 + MD5 一致 + 无中断 |
| 5 | `audio_playback` | 语音播报 | 周期 | 长时间播放不中断、设备不消失 |
| 6 | `fan` | 风扇 | 持续采样 | 转速非零、不停转 |
| 7 | `indicator_led` | 指示灯 | 持续 | 状态正确（并入终检确认） |
| 8 | `battery_soc` | 电量（充电口径） | 周期采样 | 接充电器下 SOC 不下降 |
| 9 | `lvgl_display` | 设备屏显示 | 持续 | 显示不卡死（可选，受 `enableLvgl` 控制） |
| 10 | `aging_cleanup` | 清理（删 `media/`） | 结束时一次 | 不留残留、不影响出货状态 |

### 7.0 覆盖范围声明

老化**不覆盖** HDMI：长跑期间无法可靠判定 HDMI 输出，且没有自动化的画面校验手段。
该结论必须写进报告（见 §11），避免把"未测"读成"通过"。

## 8. 各测试项设计要点

### 8.1 摄像头录存 H264 到 eMMC + 水位删除 `camera_record_emmc`

这是老化里唯一"真写数据"的重负载项，也是最能暴露长跑问题的项。

- 摄像头**真实拉流 → 真实编码 H264 → 写 eMMC**，禁止使用预生成文件或内存环回替代。
- 落盘路径固定为 `/userdata/factory_test/aging/media/rec/`，不写其它目录（见 5.2）。
- 分段录制（便于删除与校验），每段结束做一次完整性校验（CRC / 帧数 / 时长）。
- **水位删除**：剩余空间低于阈值时删除最老分段，验证长时间"写满—删除—再写"的循环不崩。
- 记录字段：分辨率、帧率、**实测码率**、累计写入量、分段数、CRC 失败数、删除触发次数、
  **每轮耗时**、完成轮数、最终剩余空间。
- 关键失败：录像中断、eMMC I/O error、删除失败、清理失败。

写入量估算（按 1080p30）：

| 码率 | 8 h | 24 h | 72 h |
|---|---:|---:|---:|
| 6 Mbps | ≈ 22 GB | ≈ 65 GB | ≈ 194 GB |

实测码率前先按 6 Mbps 估算，标定后以实测值为准，并与器件 TBW 比对（见 §14）。

### 8.2 CPU / 内存 30% 占用 `cpu_load` / `mem_load`

- 目标是**整机占用率 30%**，且是**"水位"不是"压力"**：要稳定维持在 30%，不是打满。
  因此需要闭环调节（按采样反馈调整负载强度），而不是开一个死循环。
- **第三方进程会挤占这个口径**——实施前必须实测空载基线，否则 30% 的起点就是错的。
- 内存分配**必须防 OOM**：按可用内存比例申请并留安全余量，禁止"申请到失败为止"。
- 关键失败：进程退出且未恢复、占用率长时间为 0 或长时间远超目标。

### 8.3 网口文件传输 `ethernet_transfer` + NetSinkService

自研 NetSink，不用 FTP / HTTP / scp，原因是要"边收边丢 + 校验"，且要能承受 32 路并发。

| 方面 | 做法 |
|---|---|
| **边收边丢** | 每连接固定 256KB 缓冲，读满算一次 MD5 即丢弃，**零落盘**，32 路仅约 10MB 内存 |
| 并发限流 | `SemaphoreSlim` 限 6~8 路，超出回 `busy` + 退避重试 |
| 阈值 | 用**相对值**（链路带宽 × 系数 / 并发数），不写死绝对值，避免集体假失败 |
| 记录 | 写 `netsink-<date>.jsonl`：sn、fileId、bytes、mbps、md5Match、失败原因 |
| 安全 | 仅监听内网，可选 IP 白名单，单设备配额 |

| 参数 | 取值 | 理由 |
|---|---|---|
| 单次载荷 | 1 个分段，或截取 `payloadMiB: 256` | 够大能出结论，不追求压满带宽 |
| 频次 | 30 min 一次（沿 §9 中频节奏） | 长跑中持续覆盖即可 |
| 并发 | 限流 8 路 | 载荷小，实际占用低，不需错峰 |
| 判定 | **完成 + MD5 一致 + 无中断**为主；吞吐只记录，另设宽松下限（如 ≥ 20 Mbps）兜底捕捉断流/卡死 | 不设接近链路上限的阈值 |

即网口项验证的是"长时间持续传输不中断、数据不损坏"，而不是"能跑多快"。
上行汇聚量估算：32 台 × 256 MB / 30 min，分散在 8 路并发内，千兆链路远未打满。

**降级**：服务不可达时降级为 ping 网关 + 网卡错误计数，记 `degraded` **而非 FAIL**，
否则一次上位机故障会让 32 台集体误判。

### 8.4 语音播报 `audio_playback`

周期性播放，验证音频通路长时间不中断、播放设备不消失。判定"能持续播完"，**不判音质**
（无人值守场景无法判定音质，也不该判）。

### 8.5 风扇 `fan`

持续采样转速。停转即**关键失败**（长跑中停转会引发连锁问题）。若支持调速，可做闭环验证。

### 8.6 LED 与终检

- LED 在长跑中维持应有状态，作为终检的一部分由人工确认。
- 终检做成**接口 + 两种实现**：**设备屏物理按键** / **上位机**，结果记 `manualCheckSource`。
- 两条路径写回 `result.json` 的**同一字段**，不允许出现两个终检结论。

### 8.7 电量 `battery_soc`（充电口径）

老化期间**允许接充电器**，因此电量项按**充电口径**判定：接充电器下 SOC 不应下降、能正常充电。
不再要求"拔充电器放电"——那是 PCBA / 整机阶段的动作，长跑阶段不适用。
设备屏屏幕控制有物理按键可接入，终检也不依赖拔插动作。

### 8.8 设备屏显示 `lvgl_display`

| 方面 | 做法 |
|---|---|
| 进程解耦 | LVGL 独立进程/线程，与老化**只通过 `state.json` 通信**。UI 不存在、崩溃、被 kill，老化照跑 |
| 运行时开关 | `aging.start` 带 `enableLvgl`。**一个二进制兼容带屏/不带屏两种形态**（OTA 只有一个入口，不能发两个包） |
| 语言 | **全部用英文，不额外增加字库** |

### 8.9 清理 `aging_cleanup`

老化达标后必须把设备恢复到出货状态：**不能把几十 GB 录像留给客户**。

做法依赖 §5.2 的目录隔离——所有大数据都在 `media/` 下，清理就是删这一个目录，
不做逐文件匹配（匹配式删除迟早会漏删或误删）。

```text
1. 停止所有负载（先停录制，确保没有进程还在写 media/）
2. 记录清理前可用空间
3. rm -rf /userdata/factory_test/aging/media/
4. 校验：目录已不存在 + 可用空间已回收
   （差值 ≈ 累计写入量，水位删除过的场景允许偏差，只校验"确实回收了"）
5. 写 result.json：cleanup.ok / beforeFreeGiB / afterFreeGiB / durationSec
6. 进 WAITING_MANUAL_CHECK
```

| 情况 | 处理 |
|---|---|
| 清理成功 | 正常进终检 |
| 清理失败（目录仍存在 / 空间未回收 / 超时） | **关键失败**，保留现场并告警，不允许出货 |
| **关键失败的路径** | **跳过清理**（§5.3），录像作为证据保留 |
| 人工清理 | `aging.cleanup` 命令 / 上位机"清理该槽位"按钮 |

`aging.cleanup` 支持 `scope`：

| scope | 行为 | 用途 |
|---|---|---|
| `media`（默认） | 只删 `media/` | 常规清理，自动触发即此档 |
| `all` | `media/` + 元数据一并删除 | 出货前 / 返修前彻底复位 |

**安全红线**：

- 删除目标**恒为** `media/`，不接受任何外部传入路径，不做通配符匹配
- 删除前校验路径非空且位于 `aging/` 下，防止空变量导致 `rm -rf /`
- 元数据不随 `media` 删除——`result.json` 是唯一真相源，上位机没收走前不能删

**清理失败是关键失败**：出货机器上留着几十 GB 测试残留，既是容量问题，也是隐私/合规问题。

## 9. 长跑调度分层

| 层 | 节奏 | 内容 |
|---|---|---|
| 持续 | 全程 | `camera_record_emmc`、`cpu_load`、`mem_load`、`indicator_led`、风扇/温度/SOC 采样 |
| 高频 | 每 60 s | 上报 `aging.progress`（`reportIntervalMs`），刷新 `state.json` |
| 中频 | 每 30 min | `ethernet_transfer` 一次 |
| 周期 | 可配 | `audio_playback` |
| 事件驱动 | 触发时 | 水位删除、异常记录 |
| 结束 | 一次 | 停负载 → `aging_cleanup`（删 `media/`）→ `WAITING_MANUAL_CHECK` |

长跑中没有"功能复测"环节：老化不是功能测试，重复跑功能项只会引入噪声。

## 10. 批量网络升级

升级**只在老化未启动时执行**，长跑期间禁止升级。

```text
1. 8 台一批，批次内并行
2. 每台：上传二进制（19001）→ 校验 MD5 → 停服务
3.   → .bak 备份（备份前校验非空）→ 原子 mv → start 服务
   → is-active + 19001 功能校验（get_version）→ 终态 MD5 复验
5. 任一环节失败 → 单台回滚（回滚前校验 .bak 有效），标记该槽位失败，其它槽位继续
6. 可选调用板端 save-lastgood（见 board-deployment-hardening.md）
```

- **不得跨文件系统 `mv`**，staging 必须在目标同目录
- 单台失败必须隔离，不允许"一台失败导致整批回滚"
- **升级只在老化未启动时执行**，长跑期间禁止升级
- **"升级导致的重启"不能判为故障**：主动升级引起的断连要打标记，不计入失联统计
- **升级超时配置必须单独放宽**：二进制通过 19001 上传，耗时远超普通命令

## 11. 数据记录与报告

产出以下三类文件：

- 明细：`aging-detail-<slot>-<yyyymmdd>.jsonl`
- 汇总 CSV：`aging-summary-<yyyymmdd>.csv`
- 报告 Markdown：`aging-report-<yyyymmdd>.md`

报告至少包括：

- 总时长、有效老化时长、`resumeCount` / `lostTimeSec`、**不覆盖范围声明**（HDMI）
- **`debugMode` 标记**：用调试时长跑出的报告必须标 `debug: true`，不得作为出货依据
- 每台设备：槽位、SN、IP、升级前后 MD5、**实际开始时刻 `startedAtUtc`**（批量错开启动后
  同批次必然有时间差，不记录则无法解释）、最终结论
- `camera_record_emmc`：分辨率/帧率、**实测码率**、累计写入量、分段数、CRC 失败数、
  删除触发次数、**每轮耗时**、完成轮数、最终剩余空间
- 清理结果：清理前/后可用空间、耗时、`cleanup.ok`、`media/` 残留是否存在
  （**必须进报告**——这是"设备已恢复原样"的唯一凭据，没有它就无法证明清干净了）
- 人工处置留痕：`disposedBy` / `disposedAtUtc` / `action` / `mediaRetained` / `exportPath` /
  `remark`（**失败机必填**，见 12.1——不留痕就无法解释这台为何 FAIL、现场去哪了）

## 12. 判定与失败分级

| 级别 | 示例 | 动作 |
|---|---|---|
| 瞬时 | 单次 ping 丢包、单次帧率抖动、单次扫描失败 | 记录，继续 |
| 连续 | 同项目连续 3 次失败 | 标记失败，继续收集数据 |
| 关键 | eMMC I/O error、录像中断、CPU/内存进程退出未恢复、风扇停转、网口 link 反复 flap、设备失联 > 10 min、SOC 骤降、清理失败、升级失败且回滚失败 | 立即判失败，**保留现场**，停止该槽位 |
| 安全 | 温度超限、写满导致系统分区不可用 | 立即停止负载 |
| 降级 | NetSink 服务不可达 | 记 `degraded`，不判 FAIL |

"连续"级之所以要继续收集而不是停：老化长跑中，多收集一小时的数据往往比早停一小时更有价值。
只有关键级才允许停——那时继续跑已经没有意义，且可能扩大损伤。

### 12.1 关键失败后的人工处置（SOP）

关键失败**保留现场、不自动清理**，因此必须配套定义"人来了以后做什么"。三条原则：

1. **取证在复位之前。** 现场只有一份，清掉就没了——顺序不能反。
2. **一台失败不阻塞其它槽位。** 其余 31 台照跑到各自终点，处置只针对这一台。
3. **设备没死。** 关键失败只是停止负载，命令通道仍可用，取证不需要拆机或接串口。

#### 处置前，设备处于什么状态

| 项 | 状态 |
|---|---|
| 老化状态 | `WAITING_MANUAL_CHECK`，不再计有效时长 |
| 负载 | 已全部停止，不再写盘 |
| `media/` | **原样保留**，录像分段仍在 |
| `result.json` | 已写：`failReason`、`failedAtUtc`、失败时刻各项快照 |
| 命令通道 | 可用，`status` / `result` / `cleanup` 均可响应 |
| 其它槽位 | 不受影响，继续跑 |

#### SOP

**第 0 步　上位机呈现**：槽位卡片标红并锁死"开始"，显示 槽位/SN、`failReason`、
失败时刻、已跑有效时长，以及醒目提示「`media/` 未清理，取证前请勿清理」。
顶部聚合显示「待处置 *N* 台」。

**第 1 步　取证（必做，先于任何清理）**

- 拉 `aging.result` + `events.jsonl`，按 `runId` 归档落库
- 需要原始录像时：执行"导出录像"，把 `media/rec` 分段传到上位机归档目录，按 `SN/runId` 命名
- **取证完成前禁止执行任何清理动作**

**第 2 步　定处置方式**（三选一）

| 处置 | 适用场景 | 后续动作 |
|---|---|---|
| 判 FAIL 转返修 | 硬件/确定性故障，无需再跑 | 取证 → 清理（`scope=all`）→ 贴标流转 |
| 现场分析 | 原因不明，录像有分析价值 | **保留 `media/`**，导出后交工程师；设备暂占架位 |
| 复测 | 疑似偶发（单次掉电、网口 flap） | 取证 → 清理 `media/` → 用**新 `runId`** 重新 `aging.start` |

**第 3 步　执行**

- 清理走 `aging.cleanup`（默认 `media`；返修/出货前用 `all`）
- 清理与"全部停止"同级，**需二次确认**
- 复测必须用**新 `runId`**；**`media/` 非空时 `aging.start` 返回 `busy`**（§5.5），
  必须先清理——否则两轮录像混在同一目录，分不清哪个是哪轮的

**第 4 步　复位与留痕**

处置结果写回上位机并进报告：`disposedBy` / `disposedAtUtc` / `action` /
`mediaRetained` / `exportPath` / `remark`。

不留痕等于没处置：事后无法回答"这台为什么 FAIL、现场去哪了"。处置完成后设备回 `IDLE`。

#### 现场保留时限

`media/` 保留会占几十 GB，且失败机占着架位，不能无限等。

- 配置 `retainTimeoutHours`（默认 24）：超时后上位机**只提醒，不自动删**
- **不自动删的理由**：录像可能是唯一证据，自动清理等于替人做了决定
- 确需强制清理的，由人工点按钮执行

#### 权限

| 角色 | 可做 |
|---|---|
| 操作员 | 查看失败原因、执行清理、判 FAIL 流转 |
| 工程师 | 导出录像、分析、决定是否复测 |

选择"现场分析"（保留 `media/`）需工程师确认——防止操作员随手一点毁掉证据。

## 13. 配置模型

```json
{
  "aging": {
    "station": {
      "devicePort": 19001,
      "netSinkHost": "192.168.110.2",
      "netSinkPort": 19002,
      "slots": [
        { "slot": 1, "name": "工位01", "ip": "192.168.110.101", "expectedSn": "" }
      ]
    },
    "run": {
      "totalDuration": "24h",
      "debugMode": false,
      "debugDuration": "5m",
      "batchPrepareSec": 60,
      "batchStaggerSec": 30,
      "singlePrepareSec": 10,
      "maxResumeCount": 2,
      "reportIntervalMs": 60000,
      "enableLvgl": true
    },
    "cleanup": {
      "onComplete": true,
      "retainOnFailure": true,
      "retainTimeoutHours": 24,
      "timeoutSec": 300
    },
    "upgrade": {
      "batchSize": 8,
      "timeoutSec": 600
    },
    "items": {
      "camera_record_emmc": {
        "enabled": true,
        "resolution": "1080p30",
        "segmentMinutes": 10,
        "lowWaterMarkGiB": 5,
        "maxTotalWriteGiB": 80
      },
      "cpu_load": { "enabled": true, "targetPercent": 30 },
      "mem_load": { "enabled": true, "targetPercent": 30 },
      "ethernet_transfer": {
        "enabled": true,
        "intervalMinutes": 30,
        "payloadMiB": 256,
        "maxParallel": 8,
        "minMbps": 20
      },
      "audio_playback": { "enabled": true, "intervalMinutes": 60 },
      "fan": { "enabled": true, "minRpm": 1 },
      "battery_soc": { "enabled": true, "chargeMode": true },
      "lvgl_display": { "enabled": true }
    }
  }
}
```

## 14. 风险与约束

1. **板端必须零副作用。** 常驻线程会改变 PCBA 测试环境，属于回归风险，不是实现细节。
2. **eMMC 寿命。** 按 1080p30 实测码率算总写入量（见 8.1 估算表，6 Mbps 时
   24 h ≈ 65 GB/台），再与器件 TBW 比对；32 台并行不改变单台写入量。
3. **32 台并发打流必然拥塞。** 必须限流 + 相对阈值，否则全部槽位"吞吐不达标"是误判。
4. **CPU/内存 30% 是整机占用率，且是"水位"不是"压力"。** 需闭环调节；第三方进程
   会挤占该口径，实施前必须实测空载基线；内存分配必须防 OOM。
5. **上位机全程可断。** 任何把判定或调度绑回上位机的设计，都会让一次上位机重启作废整批长跑。
6. **老化 PC 必须静态 IP。** 设备反向连 19002，地址错则整批网传项失效。
7. **升级与长跑互斥。** 长跑中升级既有风险又会污染失联统计。
8. **清理不彻底或删错路径。** 前者把几十 GB 残留留给客户，后者可能删掉系统目录。
   因此删除目标恒为固定的 `media/`、不接受外部路径、且必须做删除后校验（见 8.9）。

## 15. 实施顺序

1. `SpaceTestPC.Core` 抽取：协议模型 / 客户端 / 落库 / 配置，保证既有上位机行为不变
2. 客户端多实例化 + 每槽位独立生命周期
3. `SpaceTestPC.AgingStation` 骨架：SlotTable + 槽位网格 UI（先 mock）
4. 板端 `aging` 命令组骨架：`start` / `stop` / `status` / `result` + 懒启动 `AgingEngine`
5. 板端目录隔离（§5.2，先做）→ camera_record_emmc 落盘校验 → 水位删除 → 清理 + 删除后校验
6. 设备端 result.json + 状态机（含 WAITING_MANUAL_CHECK）
7. 上位机槽位网格 + 详情 + 报告（主入口）
8. 升级编排 8 台一批 + 单台隔离回滚
9. NetSinkService + ethernet_transfer（含 pin/unpin）
10. 启动编排：三种粒度 + 错开启动（§5.5）
11. 失败槽位呈现 + 人工处置流程（§12.1）：取证 → 清理 / 复测 + 处置留痕
12. 32 台全量试跑（先用 8h，再按需要拉到 24h / 72h）

## 16. 已确认结论

| # | 结论 |
|---|---|
| 1 | 老化模拟真实出货场景：**跑真实业务负载**，不引入合成压力工具 |
| 2 | 常温长跑，8h～72h 可配，默认 24h |
| 3 | IP 由上级路由器静态分配（MAC 绑定），老化 PC 用静态 IP，连上后校验 SN 防呆 |
| 4 | 上位机采用"共享核心 + 独立应用"（`SpaceTestPC.Core` + `SpaceTestPC.AgingStation`） |
| 5 | 监控连 32 路，升级与网传各限 6~8 路并发 |
| 6 | 控制面复用 19001 + `aging` 命令组；数据面新增 19002 自研 NetSink（边收边丢 + MD5） |
| 7 | 判定在设备端，`result.json` 为唯一真相源；上位机与 LVGL 均为读者 |
| 8 | 老化是**第三套独立形态**，不与 PCBA / 整机混用判定口径，也不引用其它方案文档 |
| 9 | 板端零副作用：未收到 `aging.start` 时不创建老化线程 |
| 10 | 老化期间**可以接充电器**，电量项按充电口径判定 |
| 11 | 设备屏幕控制**有物理按键可接入**，终检支持设备屏按键 / 上位机两种入口 |
| 12 | **LVGL 全部用英文**，不额外增加字库 |
| 13 | 判定按**累计有效运行时长**，不是墙钟时间 |
| 14 | eMMC 采用**水位删除**：写满触发删除最老分段，验证删除与清理 |
| 15 | CPU/内存 30% 是**整机占用率**，是"水位"不是"压力"，需闭环调节并实测空载基线 |
| 16 | 网传判定以**完成 + MD5 一致 + 无中断**为主，吞吐只记录并设宽松下限 |
| 17 | 老化贯穿**始终不覆盖 HDMI**，报告必须显式声明 |
| 18 | 老化结束停在 `WAITING_MANUAL_CHECK`，终检结果统一写回 `result.json` 同一字段 |
| 19 | 升级只在老化未启动时执行；单台失败隔离回滚，不允许整批回滚 |
| 20 | 升级导致的重启**不判为故障**，需打标记排除 |
| 21 | 网传服务不可达记 `degraded`，**不判 FAIL** |
| 22 | 调试时长跑出的报告必须标 `debug: true`，不得作为出货依据 |
| 23 | 老化产物独立：独立 `appsettings`、独立数据库，不混入 PCBA 库 |
| 24 | CPU/内存 30% 与判定带为**预估值**，以实测标定为准 |
| 25 | 启动支持**单槽位 / 选中 / 全部**三种粒度；批量启动时**按槽位序号依次错开**（默认 30 s/台），不做齐开 |
| 26 | 老化大数据统一落在独立的 `media/` 目录，达标后**整目录删除**恢复出货状态；**关键失败时跳过清理**，保留现场 |
| 27 | 关键失败的人工处置顺序为**先取证后清理**；处置结果必须留痕进报告；`media/` 超时**只提醒不自动删** |

## 17. 待标定 / 待确认

| # | 项 | 说明 |
|---|---|---|
| 1 | 默认总时长 | 24 h 为默认，8 h / 72 h 可配；需按产线节拍确认 |
| 2 | `camera_record_emmc` 实测码率 | 6 Mbps 为估算值（24 h ≈ 65 GB/台），必须实测后重算 |
| 3 | eMMC 水位阈值与删除触发点 | `lowWaterMarkGiB` 需结合分区大小与剩余空间实测 |
| 4 | CPU/内存 30% 的调节参数与判定带 | 需实测空载基线后标定；第三方进程会挤占该口径 |
| 5 | 72 h 场景的 `maxTotalWriteGiB` | 估算约 194 GB，需确认上调上限还是降码率 |
| 6 | `batchPrepareSec` | 需实测"32 台 `aging.start` 全部下发完"的耗时，准备窗口取其 2~3 倍 |
| 7 | `batchStaggerSec` | 默认 30 s 是按 8 h 场景估的；若跑 72 h 可上调，需确认首尾 15.5 min 偏移是否被产线接受 |
| 8 | NetSink 吞吐宽松下限 | ≥ 20 Mbps 为初值，需按专网实测链路重新标定 |
| 9 | 失败机"导出录像"的传输通道 | 建议复用 §8.3 网口通道按需导出，需板端确认可行；确定前不应依赖它做取证 |
