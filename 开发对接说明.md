# 开发对接说明

本文档是 spaceTestPC 上位机与 3576 下位机的唯一对接协议说明。通信协议、测试顺序、测试项参数、返回字段和状态规则都在本文档维护，不再单独维护 `通信协议说明.md`。

## 1. 总体架构

上位机负责：

- 扫码获取 SN。
- 查询设备端板状态。
- 必要时写入 SN。
- 下发整轮测试计划。
- 接收每个测试项的 `running / passed / failed / skipped` 事件。
- 显示测试序列、操作提示、测试明细和查询记录。
- 保存上位机数据库记录。

3576 下位机负责：

- 监听上位机连接。
- 按上位机下发的 `tests[]` 顺序执行测试。
- 每项开始和结束时上报 `test.report`。
- 某一项失败后继续执行后续测试，最终用 `session.completed` 汇总整轮结果。
- 在设备本地保存必要的板端摘要信息。

## 2. 通信方式

当前真机联调优先使用 ADB forward：

```powershell
adb forward tcp:19001 tcp:19001
```

上位机连接：

```text
127.0.0.1:19001
```

3576 端服务监听：

```text
0.0.0.0:19001
```

通信格式为 JSON Lines：

- 每条消息是一行完整 JSON。
- 使用 `\n` 结尾。
- 上位机发送请求，3576 返回一条或多条事件。

## 3. 基础命令

### 3.1 获取板状态 `sys.get_board_state`

上位机发送：

```json
{
  "protocolVersion": "1.0",
  "sessionId": "session-id",
  "sn": "S1ABCD10012",
  "commandGroup": "sys",
  "command": "get_board_state"
}
```

3576 返回：

```json
{
  "protocolVersion": "1.0",
  "requestId": "",
  "sessionId": "session-id",
  "resultCode": 0,
  "message": "Board state loaded",
  "timestamp": "2026-07-16T11:49:44+0800",
  "data": {
    "boardId": "rk3576",
    "boardSn": "S1ABCD10012",
    "testMode": "ready",
    "currentState": "idle",
    "lastSessionId": "",
    "lastStartTime": "",
    "lastEndTime": "",
    "lastVerdict": "",
    "passCount": 0,
    "failCount": 0,
    "totalCount": 0,
    "version": 1
  }
}
```

### 3.2 写入 SN `sys.write_sn`

上位机在设备 SN 为空时写入扫码 SN。

上位机发送：

```json
{
  "protocolVersion": "1.0",
  "sessionId": "session-id",
  "sn": "S1ABCD10012",
  "boardId": "rk3576",
  "commandGroup": "sys",
  "command": "write_sn"
}
```

3576 写入方式要求兼容当前板端命令：

```bash
_vendor_storage -w VENDOR_SN_ID -t string -i "S1ABCD10012"
```

规则：

- 设备 SN 为空：允许写入。
- 设备 SN 与扫码 SN 一致：允许继续。
- 设备 SN 与扫码 SN 不一致：上位机停止测试，禁止覆盖。

### 3.3 进入测试模式 `sys.enter_test_mode`

上位机发送：

```json
{
  "protocolVersion": "1.0",
  "sessionId": "session-id",
  "sn": "S1ABCD10012",
  "boardId": "rk3576",
  "commandGroup": "sys",
  "command": "enter_test_mode"
}
```

成功返回 `resultCode = 0`。

## 4. 启动整轮测试 `session.start`

上位机只发送一次整轮测试计划。3576 必须严格按 `tests[]` 数组顺序执行。

```json
{
  "protocolVersion": "1.0",
  "sessionId": "session-id",
  "sn": "S1ABCD10012",
  "boardId": "rk3576",
  "commandGroup": "session",
  "command": "start",
  "parameters": {
    "tests": [
      { "id": "board_state", "parameters": {} },
      { "id": "hdmi", "parameters": {} },
      { "id": "keys", "parameters": { "timeoutMs": 30000 } },
      { "id": "lcd", "parameters": {} },
      { "id": "ethernet", "parameters": {} },
      { "id": "wifi", "parameters": {} },
      { "id": "bluetooth", "parameters": {} },
      { "id": "fingerprint", "skip": true, "skipReason": "当前未接指纹模组", "parameters": {} },
      { "id": "typec_fast_charge", "parameters": {} },
      { "id": "typec_camera", "parameters": {} },
      { "id": "tf", "parameters": {} },
      { "id": "usb2_3", "skip": true, "skipReason": "USB2.0&3.0 汇总文件接口暂未正式启用", "parameters": {} },
      { "id": "pcba_test_points", "skip": true, "skipReason": "PCBA测试点 32通道电压采集接口暂未正式启用", "parameters": {} },
      { "id": "indicator_led", "skip": true, "skipReason": "当前指示灯测试环境暂未接入", "parameters": {} },
      { "id": "fan", "skip": true, "skipReason": "当前风扇测试环境暂未接入", "parameters": {} },
      { "id": "battery_management", "parameters": {} }
    ]
  }
}
```

## 5. 测试事件格式

### 5.1 测试项上报 `test.report`

```json
{
  "event": "test.report",
  "testId": "wifi",
  "status": "running",
  "resultCode": 0,
  "message": "Running Wi-Fi test",
  "data": {}
}
```

`status` 取值：

- `running`：测试正在执行。
- `passed`：测试通过。
- `failed`：测试失败。
- `skipped`：本轮协商不测试，不计入最终结果。

### 5.2 会话完成 `session.completed`

```json
{
  "event": "session.completed",
  "sessionId": "session-id",
  "status": "passed",
  "resultCode": 0,
  "message": "Session completed"
}
```

规则：

- 任意非 skipped 项失败，最终 `status = failed`。
- 只有 passed 和 skipped 项时，最终 `status = passed`。
- skipped 项不计入最终 PASS/FAIL。

## 6. 固定测试顺序

当前推荐顺序：

```text
board_state
-> hdmi
-> keys
-> lcd
-> ethernet
-> wifi
-> bluetooth
-> fingerprint
-> typec_fast_charge
-> typec_camera
-> tf
-> usb2_3
-> pcba_test_points
-> indicator_led
-> fan
-> battery_management
```

顺序约束：

- `board_state` 必须是第一项。
- `ethernet` 必须在 `wifi` 前执行，因为网口测试会关闭 Wi-Fi，Wi-Fi 测试会重新打开 Wi-Fi。
- `usb2_3` 放在 `tf` 后，属于外部存储和 USB 相关测试组。
- `pcba_test_points` 放在 `usb2_3` 后，属于测试夹具和外部采集协议预留项。
- skipped 项仍保留在测试序列中显示“不测试”，但不影响最终结果。

## 7. Skip 规则

上位机配置 `testPlan.skippedTests` 后，对应测试项下发：

```json
{
  "id": "pcba_test_points",
  "skip": true,
  "skipReason": "PCBA测试点 32通道电压采集接口暂未正式启用",
  "parameters": {}
}
```

3576 返回：

```json
{
  "event": "test.report",
  "testId": "pcba_test_points",
  "status": "skipped",
  "resultCode": 2900,
  "message": "PCBA测试点 32通道电压采集接口暂未正式启用",
  "data": {
    "skipReason": "PCBA测试点 32通道电压采集接口暂未正式启用",
    "countInFinalVerdict": false
  }
}
```

上位机行为：

- 测试序列中显示 skipped。
- 操作提示显示“本轮不测试”。
- 查询记录中保存 `SKIPPED`。
- 最终结果不把 skipped 当失败。

当前默认 skipped 项：

- `fingerprint`
- `usb2_3`
- `pcba_test_points`
- `indicator_led`
- `fan`

## 8. 各测试项协议

### 8.1 板状态 `board_state`

作用：

- 读取 SN。
- 读取板卡状态。
- 读取软件版本。
- 读取设备端最近一次测试摘要。

返回关键字段：

- `boardId`
- `boardSn`
- `testMode`
- `currentState`
- `lastVerdict`
- `passCount`
- `failCount`
- `totalCount`
- `warnings`

### 8.2 HDMI `hdmi`

当前为人工判定项。

3576 发送 running 后等待上位机人工判定：

```json
{
  "event": "test.report",
  "testId": "hdmi",
  "status": "running",
  "resultCode": 0,
  "message": "Waiting for HDMI manual decision",
  "data": {
    "manualObserved": true
  }
}
```

上位机发送人工判定：

```json
{
  "protocolVersion": "1.0",
  "sessionId": "session-id",
  "commandGroup": "operator",
  "command": "decision",
  "parameters": {
    "testId": "hdmi",
    "passed": true,
    "reason": "operator confirmed"
  }
}
```

### 8.3 按键 `keys`

测试按键：

- 上
- 下
- 左
- 右
- 确认

当前 3576 输入设备：

- 方向键：`/dev/input/by-path/platform-gpio-keys-event`
- 确认键：`/dev/input/by-path/platform-2ac40000.i2c-platform-rk805-pwrkey.1.auto-event`

键值映射：

- `KEY_UP` -> `up`
- `KEY_DOWN` -> `down`
- `KEY_PREVIOUS` -> `left`
- `KEY_NEXT` -> `right`
- `KEY_POWER` -> `confirm`

上位机参数：

```json
{
  "id": "keys",
  "parameters": {
    "timeoutMs": 30000
  }
}
```

3576 running 过程中可持续返回：

```json
{
  "event": "test.report",
  "testId": "keys",
  "status": "running",
  "resultCode": 0,
  "message": "Key detected: up",
  "data": {
    "key": "up",
    "detectedKeys": ["up"],
    "expectedKeys": ["up", "down", "left", "right", "confirm"],
    "allKeysDetected": false
  }
}
```

失败条件：

- 30 秒内五个按键未全部识别。

### 8.4 LCD `lcd`

当前为人工判定项。3576 端显示测试图案，上位机等待人工确认。

返回关键字段：

- `manualObserved`
- `pattern`
- `backlightOk`
- `displayOk`

### 8.5 网口 `ethernet`

网口测试必须在 Wi-Fi 测试前执行。

上位机操作提示：

- 开始：`请插入网线，系统将关闭 Wi-Fi 并检测有线网络。`
- 完成：`网口测试完成，请拔掉网线，准备进行 Wi-Fi 测试。`

上位机参数：

```json
{
  "id": "ethernet",
  "parameters": {
    "interfaceName": "end0",
    "routerIp": "192.168.110.1",
    "pingCount": 4,
    "timeoutMs": 15000,
    "waitCableUnplug": true,
    "unplugTimeoutMs": 10000
  }
}
```

3576 执行逻辑：

1. `nmcli radio wifi off`
2. 等待 `end0` link up
3. 等待获取 IP
4. `ping -I end0 192.168.110.1`
5. 提示拔掉网线
6. 如果配置 `waitCableUnplug = true`，等待 link down

返回关键字段：

- `interfaceName`
- `routerIp`
- `wifiDisabled`
- `linkUp`
- `ipAcquired`
- `pingOk`
- `ip`
- `avgDelayMs`
- `cableUnplugged`

错误码：

- `4800`：网口测试通用失败
- `4801`：关闭 Wi-Fi 失败
- `4802`：网线或 link 未就绪
- `4803`：未获取到 IP
- `4805`：ping 路由失败

### 8.6 Wi-Fi `wifi`

3576 使用 `nmcli` 开发。

上位机参数：

```json
{
  "id": "wifi",
  "parameters": {
    "ssid": "originflow",
    "password": "test_router_001",
    "routerIp": "192.168.110.1",
    "pingCount": 4,
    "timeoutMs": 15000
  }
}
```

3576 执行逻辑：

1. `nmcli radio wifi on`
2. 连接目标 SSID
3. 获取无线网卡 IP
4. 使用无线网卡 ping 路由器，避免误走有线链路

返回关键字段：

- `ssid`
- `routerIp`
- `connected`
- `linked`
- `ip`
- `pingOk`
- `avgDelayMs`

### 8.7 蓝牙 `bluetooth`

3576 使用 `bluetoothctl` 开发。

当前测试目标：

- 扫描 `Mate40`
- 扫描到即通过

上位机参数：

```json
{
  "id": "bluetooth",
  "parameters": {
    "mode": "observer",
    "targetName": "Mate40",
    "minRssi": -80,
    "scanWindowMs": 10000
  }
}
```

返回关键字段：

- `targetName`
- `found`
- `rssi`
- `minRssi`
- `scanWindowMs`

### 8.8 指纹模组 `fingerprint`

当前未接指纹模组，默认配置为 skipped。

如果取消 skip，当前 3576 返回 failed：

```json
{
  "event": "test.report",
  "testId": "fingerprint",
  "status": "failed",
  "resultCode": 4101,
  "message": "Fingerprint module is not implemented on 3576 yet",
  "data": {
    "implemented": false
  }
}
```

### 8.9 板快充 `typec_fast_charge`

目标是读取充电输入电压和充电电流。

当前定位：

- 主要检测充电时输入电压是否满足要求。
- 未来通过文件接口读取。
- `bq2579x` 负责快充/充电管理相关信息。
- `cw221x-bat` 更偏电池电量/电池状态，不是输入快充电压电流的主要来源。

上位机参数：

```json
{
  "id": "typec_fast_charge",
  "parameters": {
    "batterySimulationVoltageMv": 7400,
    "chargeVoltageMinMv": 7400,
    "chargeVoltageMaxMv": 8400,
    "chargeCurrentMinMa": 500,
    "chargeCurrentMaxMa": 3000,
    "stableSampleCount": 3,
    "sampleIntervalMs": 500,
    "timeoutMs": 20000
  }
}
```

返回关键字段：

- `chargerConnected`
- `chargeVoltageMv`
- `chargeCurrentMa`
- `stable`
- `voltageMinMv`
- `voltageMaxMv`
- `currentMinMa`
- `currentMaxMa`

### 8.10 TYPE-C 相机 `typec_camera`

当前底层判定：

- 能拉到流。
- 未来增加曝光中断检测。
- 目标帧率按 30fps 判断。

上位机参数：

```json
{
  "id": "typec_camera",
  "parameters": {
    "streamProfile": "1080p30",
    "minInterruptCount": 200,
    "timeoutMs": 10000
  }
}
```

返回关键字段：

- `streaming`
- `capturedFrames`
- `frameRate`
- `interruptCount`
- `exposureDelta`
- `streamOk`
- `exposureOk`

### 8.11 TF 卡 `tf`

目标：

- 自动挂载 `/dev/mmcblk1p1`
- 如果文件系统不符合要求，可格式化为 ext4
- 最终返回上位机需要显示的信息

上位机参数：

```json
{
  "id": "tf",
  "parameters": {
    "devicePath": "/dev/mmcblk1p1",
    "mountPoint": "/mnt/spacetest_tf",
    "allowFormatExt4": true,
    "minCapacityMb": 1,
    "mountTimeoutMs": 5000
  }
}
```

返回关键字段：

- `devicePath`
- `mountPoint`
- `mounted`
- `formatted`
- `fileSystem`
- `capacityMb`
- `readWriteOk`

### 8.12 USB2.0&3.0 `usb2_3`

当前默认 skipped。未来由设备端维护 USB 插拔记录汇总文件。

上位机操作提示：

```text
请确保测试前已经使用 2.0 U盘和 3.0 U盘插入过需要测试的 USB 口，并已经生成 USB 汇总文件。
```

上位机参数：

```json
{
  "id": "usb2_3",
  "parameters": {
    "recordFile": "/tmp/spacetest_usb_ports.json",
    "expectedUsb2Count": 2,
    "expectedUsb3Count": 2,
    "timeoutMs": 3000
  }
}
```

3576 当前读取文件格式：

```json
{
  "usb2Count": 2,
  "usb3Count": 2,
  "ports": []
}
```

返回关键字段：

- `recordFile`
- `usb2Count`
- `usb3Count`
- `expectedUsb2Count`
- `expectedUsb3Count`

错误码：

- `4900`：汇总文件格式无效
- `4901`：汇总文件不存在或超时
- `4902`：USB2.0 插入记录不足
- `4903`：USB3.0 插入记录不足

### 8.13 PCBA测试点 `pcba_test_points`

当前默认 skipped。协议先打通 32 通道电压返回链路，后续底层可替换为真实 ADC 或测试夹具。

上位机操作提示：

```text
正在读取 PCBA 32 通道测试点电压，系统将自动判断是否在阈值范围内。
```

上位机参数：

```json
{
  "id": "pcba_test_points",
  "parameters": {
    "channelCount": 32,
    "recordFile": "/tmp/spacetest_pcba_points.json",
    "defaultMinMv": 0,
    "defaultMaxMv": 5000,
    "timeoutMs": 5000,
    "channels": []
  }
}
```

3576 当前读取文件格式：

```json
{
  "channelCount": 32,
  "points": [
    { "index": 1, "voltageMv": 3300 },
    { "index": 2, "voltageMv": 1800 }
  ]
}
```

3576 返回关键字段：

- `channelCount`
- `parsedCount`
- `passedCount`
- `failedCount`
- `failedPoints`
- `points[]`

`points[]` 单项格式：

```json
{
  "index": 1,
  "name": "TP01",
  "voltageMv": 3300,
  "minMv": 0,
  "maxMv": 5000,
  "passed": true
}
```

错误码：

- `5000`：PCBA 测试点文件格式无效
- `5001`：PCBA 测试点文件不存在或超时
- `5002`：通道数量或电压数量不足
- `5003`：至少一个测试点电压超出阈值
- `5004`：预留给底层采集接口异常

### 8.14 指示灯板 `indicator_led`

当前默认 skipped。

保留框架：

- `/sys/class/leds/ledb/brightness`
- `/sys/class/leds/ledg/brightness`

未来接入电压检测仪后，由上位机或下位机联动判断电压变化是否满足要求。

### 8.15 风扇 `fan`

当前默认 skipped。未来接入风扇供电、电压或转速检测。

### 8.16 板放电 `battery_management`

目标是读取整板放电时的电压和电流。

上位机参数：

```json
{
  "id": "battery_management",
  "parameters": {
    "batterySimulationVoltageMv": 7400,
    "dischargeVoltageMinMv": 7000,
    "dischargeVoltageMaxMv": 7600,
    "dischargeCurrentMinMa": 100,
    "dischargeCurrentMaxMa": 1500,
    "stableSampleCount": 3,
    "sampleIntervalMs": 500,
    "timeoutMs": 15000
  }
}
```

返回关键字段：

- `dischargeVoltageMv`
- `dischargeCurrentMa`
- `stable`

## 9. 上位机配置位置

上位机配置文件：

```text
SpaceTestPC.App/appsettings.json
```

关键配置：

- `testPlan.enabledTests`
- `testPlan.disabledTests`
- `testPlan.skippedTests`
- `testPlan.testParameters`

当前 skipped 配置示例：

```json
"skippedTests": {
  "indicator_led": "当前指示灯测试环境暂未接入",
  "fan": "当前风扇测试环境暂未接入",
  "fingerprint": "当前未接指纹模组",
  "usb2_3": "USB2.0&3.0 汇总文件接口暂未正式启用",
  "pcba_test_points": "PCBA测试点 32通道电压采集接口暂未正式启用"
}
```

## 10. 3576 端代码结构

3576 项目目录：

```text
/home/originflow/work/spaceTest3576
```

主要结构：

```text
config/                 配置
manage/                 会话管理
protocol/               JSON Lines 协议封装
storage/                板状态和本地记录
tests/                  测试计划执行
hardware/
  keys/                 按键
  wifi/                 Wi-Fi
  bluetooth/            蓝牙
  ethernet/             网口
  fingerprint/          指纹模组
  fast_charge/          板快充
  camera/               TYPE-C 相机
  tf_card/              TF 卡
  usb3.0/               USB2.0&3.0 汇总文件检测
  pcba_points/          PCBA 32 通道测试点
  indicator_led/        指示灯板
```

## 11. 当前已知限制

- 指纹模组当前未接，默认 skipped；取消 skip 后当前返回 failed。
- 指示灯板、风扇当前未接完整检测环境，默认 skipped。
- USB2.0&3.0 当前等待设备端生成汇总文件，默认 skipped。
- PCBA测试点当前通过汇总文件验证 32 通道链路，真实采集接口后续替换。
- 网口测试会关闭 Wi-Fi，不建议通过 Wi-Fi SSH 会话直接跑真实网口测试；应通过 ADB 或上位机流程验证。

## 12. 验证命令

上位机测试：

```powershell
dotnet test SpaceTestPC.slnx --no-restore -p:BaseOutputPath=artifacts\testbin\
```

3576 编译：

```bash
cd /home/originflow/work/spaceTest3576
make clean
make
```

3576 服务启动：

```bash
cd /home/originflow/work/spaceTest3576
nohup ./spacetest3576 >/tmp/spacetest3576.log 2>&1 < /dev/null &
```

