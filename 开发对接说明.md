# 开发对接说明

更新日期：2026-07-14

## 1. 当前架构

上位机采用统一会话协议。扫码后只发送一次 `session.start`，PCBA 按计划执行并持续上报测试状态；上位机不再按测试点分别下发蓝牙、WiFi、网口等命令。

上位机职责：显示状态、接收结果、失败后停止等待、保存完整记录和导出 CSV。

设备端职责：执行测试、上报完整结果，并在本地只保存每个测试项的简要结论；不保存 WiFi、蓝牙等测试明细。

PCBA 职责：按顺序执行测试、每项开始/结束上报、任一项失败后停止后续项并上报整轮完成。

## 2. 统一协议

### 2.1 启动会话

上位机通过 ADB 转发后的 TCP 长连接发送一行 JSON：

```json
{
  "protocolVersion": "1.0",
  "sessionId": "uuid",
  "sn": "SN123456",
  "commandGroup": "session",
  "command": "start",
  "parameters": {
    "tests": [
      { "id": "board_state", "parameters": {} },
      { "id": "hdmi", "parameters": {} },
      { "id": "keys", "parameters": {} },
      { "id": "lcd", "parameters": {} },
      { "id": "wifi", "parameters": {} },
      { "id": "bluetooth", "parameters": {} },
      { "id": "fingerprint", "parameters": {} },
      { "id": "typec_fast_charge", "parameters": {} },
      { "id": "typec_camera", "parameters": {} },
      { "id": "tf", "parameters": {} },
      { "id": "indicator_led", "parameters": {} },
      { "id": "fan", "parameters": {} },
      { "id": "otg", "parameters": {} },
      { "id": "battery_management", "parameters": {} }
    ]
  }
}
```

### 2.2 测试点上报

PCBA 在同一连接中逐行上报：

```json
{
  "event": "test.report",
  "testId": "bluetooth",
  "status": "running",
  "resultCode": 0,
  "message": "scan started",
  "data": {},
  "timestamp": "2026-07-14T10:00:00+08:00"
}
```

结束状态只能是 `passed` 或 `failed`。失败后 PCBA 不得继续执行下一项。

### 2.3 会话完成上报

```json
{
  "event": "session.completed",
  "status": "passed",
  "resultCode": 0,
  "message": "all tests passed"
}
```

失败时 `status` 为 `failed`，并在 `message` 和 `data` 中给出失败原因与失败测试点。

## 3. 固定测试点 ID

固定顺序：
`board_state`、`hdmi`、`keys`、`lcd`、`wifi`、`bluetooth`、`fingerprint`、`typec_fast_charge`、`typec_camera`、`tf`、`indicator_led`、`fan`、`otg`、`battery_management`。

测试说明：
- `board_state`：板状态，作为第一项，确认板卡身份和可测试状态。
- `hdmi`：HDMI，人工眼睛观测。
- `keys`：按键，input 子系统测试，人工参与。
- `lcd`：SPI LCD 屏，走 SPI 测试。
- `wifi`：连接并 ping 路由器，SSID/code 均为 `test_router_001`。
- `bluetooth`：观察者模式扫描指定蓝牙名称。
- `fingerprint`：SPI 指纹模组，走 SPI 测试。
- `typec_fast_charge`：TYPE-C 充电测试，电池模拟器模拟电池，PCBA 通过 I2C PMIC 上报充电电压和电流，上位机自动判定。
- `typec_camera`：TYPE-C 相机，拉流并测试中断数。
- `tf`：TF 卡，读取 TF 卡信息。
- `indicator_led`：指示灯板，通过电压测试仪测试。
- `fan`：风扇，通过电压测试仪测试。
- `otg`：USB OTG 口，插 U 盘，写入系统文件后读取。
- `battery_management`：电池放电测试，电池模拟器读取整板消耗电压和电流。

各测试点的特有字段统一放在 `parameters` 和 `data` 中，不改变公共事件结构。

## 4. 当前实现状态

- `IPcbaCommandClient.RunSessionAsync` 是统一会话接口。
- `MockPcbaCommandClient` 会对完整测试计划按顺序发送 `running -> passed`，最终发送 `session.completed: passed`。
- `AdbPcbaCommandClient` 会建立一次 ADB 转发 TCP 连接，发送 `session.start` 后持续读取事件，直到 `session.completed`。
- 上位机收到 `failed` 后显示失败；底层完成事件到达后保存本轮记录。
- 每轮记录写入 `data/stage1-db.json`，同时追加摘要到 `data/stage1-db.csv`。

## 5. 真机对接前必须确认

统一外层协议和全部 `testId` 已定，但各项真实硬件参数与 `data` 明细尚未定稿。PCBA 需逐项确认：

- `parameters` 参数名、类型和默认值。
- `data` 返回字段、单位和成功判定。
- 超时、失败 `resultCode` 和错误文本。
- 失败后停止策略及 `session.completed` 的失败内容。

确认后将 `MainViewModel.cs` 中的 `ConnectionMode` 从 `Mock` 改为 `AdbForward` 即可进入真机联调。

## 6. `board_state` 板状态协议

`board_state` 是整轮测试第一项，用于采集板卡身份、软件版本、本地测试记录和当前可测试状态。该项默认不做强制拦截判断；只要设备能正常返回基础信息，上位机应继续执行后续测试。版本不一致、本地记录缺失等问题通过 `warnings` 字段展示。

### 6.1 上位机下发

`board_state` 不需要单独命令。上位机在 `session.start` 的 `tests` 数组中把它放在第一项：

```json
{
  "id": "board_state",
  "parameters": {
    "includeLocalRecord": true,
    "includeVersion": true,
    "includeCounters": true
  }
}
```

字段说明：
- `includeLocalRecord`：是否读取设备本地最近一次测试记录。
- `includeVersion`：是否读取 `uboot/kernel/rootfs` 版本。
- `includeCounters`：是否读取本地累计测试次数。

### 6.2 设备上报 running

设备开始采集板状态时，先上报：

```json
{
  "event": "test.report",
  "testId": "board_state",
  "status": "running",
  "resultCode": 0,
  "message": "collecting board state",
  "data": {},
  "timestamp": "2026-07-14T10:00:00+08:00"
}
```

### 6.3 设备上报 passed

正常采集完成后，上报：

```json
{
  "event": "test.report",
  "testId": "board_state",
  "status": "passed",
  "resultCode": 0,
  "message": "Board info collected",
  "data": {
    "sn": "SN123456",
    "currentState": "idle",
    "ubootVersion": "2024.01",
    "kernelVersion": "6.1.0",
    "rootfsVersion": "1.0.3",
    "appVersion": "0.1.0",
    "localRecordFound": true,
    "localRecordWritable": true,
    "lastTestVerdict": "Pass",
    "lastTestTime": "2026-07-14T09:30:00+08:00",
    "passCount": 12,
    "failCount": 1,
    "totalCount": 13,
    "warnings": []
  },
  "timestamp": "2026-07-14T10:00:02+08:00"
}
```

### 6.4 非强制告警

如果本地记录不存在、版本字段为空、计数读取失败，但板子仍可继续测试，仍然返回 `status: "passed"`，并通过 `resultCode` 和 `warnings` 展示：

```json
{
  "event": "test.report",
  "testId": "board_state",
  "status": "passed",
  "resultCode": 1001,
  "message": "Board info collected with warnings",
  "data": {
    "sn": "SN123456",
    "currentState": "idle",
    "ubootVersion": "2024.01",
    "kernelVersion": "6.1.0",
    "rootfsVersion": "1.0.3",
    "localRecordFound": false,
    "localRecordWritable": true,
    "warnings": [
      "local_test_record_missing"
    ]
  },
  "timestamp": "2026-07-14T10:00:02+08:00"
}
```

建议保留的 `warnings` 取值：
- `local_test_record_missing`：设备本地没有测试记录。
- `local_test_record_read_failed`：设备本地记录读取失败。
- `local_test_record_write_unavailable`：设备本地记录不可写。
- `version_field_missing`：版本字段缺失。
- `sn_missing`：SN 为空。
- `sn_mismatch`：上位机 SN 与设备本地 SN 不一致。

### 6.5 强制失败场景

仅通信或基础状态无法满足测试时，才返回 `failed`，并终止后续测试：

```json
{
  "event": "test.report",
  "testId": "board_state",
  "status": "failed",
  "resultCode": 2001,
  "message": "board state unavailable",
  "data": {
    "reason": "device_state_busy"
  },
  "timestamp": "2026-07-14T10:00:02+08:00"
}
```

建议强制失败原因：
- `device_not_ready`：设备未准备好。
- `device_state_busy`：设备正处于其他测试或业务状态。
- `board_info_timeout`：板状态读取超时。
- `board_info_protocol_error`：设备返回格式错误。

## 7. 设备本地测试记录

设备本地每轮测试结束后应保存最近一次测试记录。上位机下一次执行 `board_state` 时读取并展示。

建议设备本地保存 JSON：

```json
{
  "sn": "SN123456",
  "sessionId": "abc123",
  "startTime": "2026-07-14T10:00:00+08:00",
  "endTime": "2026-07-14T10:01:00+08:00",
  "finalVerdict": "Pass",
  "failedTestId": "",
  "software": {
    "uboot": "2024.01",
    "kernel": "6.1.0",
    "rootfs": "1.0.3"
  },
  "summary": {
    "passCount": 12,
    "failCount": 1,
    "totalCount": 13
  }
}
```

本地记录保存失败不影响上位机本地 JSON/CSV 保存，但设备应在下一次 `board_state.data.warnings` 中上报 `local_test_record_write_unavailable`。

## 8. 通信传输方式

应用层统一使用 JSON Lines：每条消息是一行完整 JSON，以换行符 `\n` 结束。不同传输方式只影响连接方式，不影响 JSON 字段。

推荐支持顺序：
- `Mock`：上位机本地模拟，不连接设备。
- `AdbForwardTcp`：ADB 端口转发后的 TCP 长连接，真机联调首选。
- `TcpDirect`：设备和上位机在同一网络时直连 TCP。
- `UsbCdc`：USB 虚拟串口可选，只传命令和测试结果，不作为单独日志串口。

设备端只需要实现同一套消息：
- 接收 `session.start`。
- 按顺序上报 `test.report`。
- 任一测试失败后停止后续测试。
- 最后上报 `session.completed`。

## 9. 上位机本地配置

上位机启动时优先读取 exe 同目录的 `appsettings.jsonc`，如果不存在则回退读取 `appsettings.json`。该配置只影响上位机本轮下发的测试计划和 Mock 行为，不改变统一通信协议。

说明：
- `appsettings.json`：标准 JSON，不支持注释。
- `appsettings.jsonc`：允许写 `//` 注释，推荐开发阶段使用。
- 输出目录会同时带上 `appsettings.json` 和 `appsettings.example.jsonc` 作为参考。

默认配置：

```json
{
  "testPlan": {
    "enabledTests": [],
    "disabledTests": [],
    "mock": {
      "failingTestId": null,
      "runningDelayMs": 2000,
      "resultHoldMs": 1000,
      "boardStateRunningDelayMs": 3000,
      "boardStateResultHoldMs": 5000
    }
  }
}
```

测试项过滤规则：
- `enabledTests` 非空时，只运行其中指定的测试项，并按固定测试顺序执行。
- `enabledTests` 为空时，运行完整测试计划，再排除 `disabledTests`。
- `board_state` 默认自动保留为第一项；即使 `enabledTests` 未填写，也会自动补上。
- 配置文件不存在或读取失败时，按完整测试计划执行。
- `testParameters` 中以测试 ID 为键的对象，会原样写入本轮 `session.start.tests[].parameters`；例如 `testParameters.wifi` 就是待测设备连接路由器和 Ping 所使用的参数。

只开发 WiFi 时可配置：

```json
{
  "testPlan": {
    "enabledTests": [
      "board_state",
      "wifi"
    ],
    "disabledTests": [],
    "mock": {
      "failingTestId": null,
      "runningDelayMs": 2000,
      "resultHoldMs": 1000,
      "boardStateRunningDelayMs": 3000,
      "boardStateResultHoldMs": 5000
    }
  }
}
```

模拟 WiFi 失败：

```json
{
  "testPlan": {
    "enabledTests": [
      "board_state",
      "wifi"
    ],
    "mock": {
      "failingTestId": "wifi"
    }
  }
}
```

## 10. `keys` 方向键人工确认协议

### 10.0 人工测试统一结果闭环

HDMI（`hdmi`）和 SPI LCD（`lcd`）由工程师在上位机界面判定，但上位机点击结果后不能自行将该项目置为最终通过或失败。上位机必须向设备发送 `operator.decision`；设备收到后负责上报最终 `test.report`，再由上位机更新 UI、保存 JSON/CSV 记录。

| 测试项 | 上位机发送的 `testId` | 设备最终上报 | 失败后的设备行为 |
| --- | --- | --- | --- |
| HDMI | `hdmi` | `test.report / hdmi / passed` 或 `failed` | `session.completed / failed`，停止后续项 |
| 按键 | 无人工判定 | `test.report / keys / passed` 或 `failed` | `session.completed / failed`，停止后续项 |
| SPI LCD | `lcd` | `test.report / lcd / passed` 或 `failed` | `session.completed / failed`，停止后续项 |

HDMI 与 SPI LCD 以各自的 `testId` 关联，人工判定互不影响；按键无需人工判定。

`keys` 用于测试 PCBA 的四个方向键，固定值为 `up`、`down`、`left`、`right`。设备开始测试后必须保持 `test.report` 为 `running`，每识别到一个有效按键即上报一次。上位机累计 `detectedKeys` 并实时点亮对应按键；四键都识别后，设备自动上报最终通过，不需要人工判定。

### 10.1 会话下发

上位机在 `session.start.parameters.tests` 中下发：

```json
{
  "id": "keys",
  "parameters": {
    "expectedKeys": ["up", "down", "left", "right"],
    "inputSubsystem": "evdev"
  }
}
```

### 10.2 设备按键过程上报

每次上报均为一行 JSON，并以 `\n` 结尾。`key` 是本次新触发的按键；`detectedKeys` 必须包含截至本次已确认的全部按键，便于上位机断线恢复页面状态。

```json
{
  "event": "test.report",
  "testId": "keys",
  "status": "running",
  "resultCode": 0,
  "message": "key detected",
  "data": {
    "inputSubsystem": "evdev",
    "key": "left",
    "detectedKeys": ["up", "down", "left"],
    "expectedKeys": ["up", "down", "left", "right"],
    "allKeysDetected": false
  },
  "timestamp": "2026-07-14T10:00:00+08:00"
}
```

重复按键允许上报，但不会改变上位机已点亮的状态。设备不得在四键完成前自行上报最终 `passed`；四键完成后应立即上报 `test.report / keys / passed` 并继续下一项。若超时或 input 子系统异常，设备上报 `keys / failed`，随后上报 `session.completed / failed`。

Mock 模式按 `up -> down -> left -> right` 每 2 秒发送一次过程上报，四键完成后自动上报 `keys / passed`。

## 11. `lcd` SPI 屏人工确认

`lcd` 当前采用人工观察：设备启动 SPI LCD 并显示 RGB 测试图案后，上报 `test.report` 的 `running` 状态，`data` 至少包含 `bus: "spi"`、`pattern: "rgb"` 和 `manualObserved: true`。上位机提示工程师检查背光、RGB 图案完整性、稳定性，以及是否存在花屏、缺线或闪烁。

工程师通过同一长连接发送 `operator.decision`，其中 `testId` 固定为 `lcd`。通过后设备上报 `lcd / passed` 并继续；失败后上报 `lcd / failed` 及 `session.completed / failed`。该判定与 `hdmi`、`keys` 分别以各自的 `testId` 隔离。

## 12. `wifi` 自动测试协议

`wifi` 为设备自动测试，不需要上位机人工确认。设备连接指定路由器并完成 DHCP 后，Ping 路由器；连接失败、未拿到 IPv4 地址或 Ping 失败均为失败。

上位机从本地配置的 `testPlan.testParameters.wifi` 读取以下参数，并原样下发到 `session.start` 的 `wifi.parameters`；设备不得依赖写死的 SSID 或网关。

### 12.1 会话下发

```json
{
  "id": "wifi",
  "parameters": {
    "ssid": "test_router_001",
    "password": "test_router_001",
    "routerIp": "192.168.1.1",
    "pingCount": 4,
    "timeoutMs": 15000
  }
}
```

`routerIp` 在现场路由器不是 `192.168.1.1` 时修改为实际网关地址。

### 12.2 结果上报

设备必须分阶段上报：开始关联时 `stage: connecting`，获取 IP 后 `stage: pinging`，最终再上报 `passed/failed`。关联和 Ping 阶段均使用 `test.report / wifi / running`，使上位机可显示当前 SSID、IP 和网关。

```json
{
  "event": "test.report",
  "testId": "wifi",
  "status": "running",
  "message": "WiFi connected, pinging router",
  "data": {
    "stage": "pinging",
    "ssid": "test_router_001",
    "connected": true,
    "ip": "192.168.1.20",
    "routerIp": "192.168.1.1"
  }
}
```

最终上报必须包含以下字段：

```json
{
  "event": "test.report",
  "testId": "wifi",
  "status": "passed",
  "resultCode": 0,
  "message": "WiFi router ping passed",
  "data": {
    "ssid": "test_router_001",
    "connected": true,
    "ip": "192.168.1.20",
    "routerIp": "192.168.1.1",
    "pingOk": true,
    "avgDelayMs": 12,
    "pingCount": 4
  },
  "timestamp": "2026-07-14T10:00:00+08:00"
}
```

通过条件：`connected`、`pingOk` 均为 `true`，且 `ip` 为有效 IPv4 地址。失败时 `status: "failed"`，`data` 至少回传上述布尔状态、已获取的 IP（如有）以及 `failureReason`；设备随后停止后续测试并上报 `session.completed / failed`。

## 13. `bluetooth` 目标广播扫描协议

`bluetooth` 为设备自动测试。上位机从 `testPlan.testParameters.bluetooth` 读取目标广播名称和 RSSI 阈值，并写入 `session.start.tests[].parameters`；设备必须按收到的参数扫描，不能依赖写死的目标名称。

### 13.0 上位机蓝牙广播模块

上位机启动时配置连接在 `COM6` 的 EWM104-BT57U 模块：串口参数固定为 `115200/8N1`，依次发送 `AT+ROLE=1`、`AT+NAME=<broadcastName>`、`AT+ADV=1,0,500`，每条命令必须收到 `+OK`。广播模块的串口和广播名称在 `appsettings.json.bluetoothBroadcaster` 配置。上位机以该 `broadcastName` 自动覆盖蓝牙测试下发的 `targetName`，确保 PCBA 始终扫描当前实际广播的设备。

### 13.1 会话下发

```json
{
  "id": "bluetooth",
  "parameters": {
    "mode": "observer",
    "targetName": "NODE_A_01",
    "minRssi": -80,
    "scanWindowMs": 10000
  }
}
```

- `mode`：固定为 `observer`。
- `targetName`：必须精确匹配的蓝牙广播名称。
- `minRssi`：最小可接受信号强度，单位 dBm。
- `scanWindowMs`：最大扫描时长，单位毫秒。

### 13.2 结果上报与记录

设备开始扫描时上报 `stage: scanning`，并携带目标名称、模式和 RSSI 阈值；发现目标时继续上报 `stage: target_found` 与实时 RSSI，结束后再上报最终结果。所有过程事件使用 `test.report / bluetooth / running`。

```json
{
  "event": "test.report",
  "testId": "bluetooth",
  "status": "running",
  "message": "Target Bluetooth advertisement found",
  "data": {
    "stage": "target_found",
    "mode": "observer",
    "targetName": "NODE_A_01",
    "found": true,
    "rssi": -62
  }
}
```

结束后上报：

```json
{
  "event": "test.report",
  "testId": "bluetooth",
  "status": "passed",
  "resultCode": 0,
  "message": "Target Bluetooth name scanned",
  "data": {
    "mode": "observer",
    "targetName": "NODE_A_01",
    "scanWindowMs": 10000,
    "found": true,
    "rssi": -62,
    "minRssi": -80
  },
  "timestamp": "2026-07-14T10:00:00+08:00"
}
```

通过条件为 `found: true` 且 `rssi >= minRssi`。未发现、扫描超时或 RSSI 不达标时上报 `failed`，`data.failureReason` 取 `target_not_found`、`scan_timeout` 或 `rssi_below_threshold`，然后停止后续测试并上报 `session.completed / failed`。

双端记录要求：设备在上报最终 `passed` 或 `failed` 前，只将测试摘要写入本地记录：`sessionId`、`sn`、`testId`、`verdict`、`resultCode`、`timestamp`。不保存目标名、RSSI、WiFi IP、耗时等测试明细。上位机收到最终 `test.report` 后，将完整 `data` 保存到本机 `data/stage1-db.json` 的 `testResults`，并在 CSV 的 `test_results` 列写入各项目状态摘要。该规则适用于所有测试项；设备后续在 `board_state` 中仅回传本地记录的汇总或最近结论。

## 14. `fingerprint` SPI 指纹模组自动测试协议

`fingerprint` 为设备自动测试。上位机只下发通用 SPI 命令，设备负责与指纹模组通信并依据实际返回自动判定；上位机不解析模组私有协议，也不提供人工确认。

### 14.1 会话下发

```json
{
  "id": "fingerprint",
  "parameters": {
    "bus": "spi",
    "command": "get_status",
    "timeoutMs": 3000
  }
}
```

- `bus`：当前固定为 `spi`。
- `command`：通用请求名，当前为 `get_status`；设备可在内部转换为模组私有命令。
- `timeoutMs`：通信和结果返回的最大时长。

### 14.2 结果上报

设备开始执行时上报 `test.report / fingerprint / running`。最终通过条件是 SPI 通信成功且识别到模组：`communicationOk: true`、`sensorDetected: true`。

```json
{
  "event": "test.report",
  "testId": "fingerprint",
  "status": "passed",
  "resultCode": 0,
  "message": "SPI fingerprint module passed",
  "data": {
    "bus": "spi",
    "command": "get_status",
    "timeoutMs": 3000,
    "communicationOk": true,
    "sensorDetected": true
  },
  "timestamp": "2026-07-14T10:00:00+08:00"
}
```

SPI 超时、通信错误或未检测到模组时，设备上报 `failed`，并在 `data.failureReason` 返回 `spi_timeout`、`spi_communication_error` 或 `sensor_not_detected`；然后停止后续测试并上报 `session.completed / failed`。设备本地只保存该项的通过或失败摘要，上位机保存完整上报数据。

## 15. `typec_fast_charge` TYPE-C 充电测试协议

该项使用 JK5506 电池模拟器模拟电池，例如设定为 `7400 mV`。进入该项时上位机提示“请插入 TYPE-C 充电器”，并通过 JK5506 设置模拟电池电压、开启输出。PCBA 通过 I2C 从电源管理芯片读取充电电压和充电电流；上位机依据配置范围自动判定。

### 15.1 会话下发

```json
{
  "id": "typec_fast_charge",
  "parameters": {
    "batterySimulationVoltageMv": 7400,
    "chargeVoltageMinMv": 7400,
    "chargeVoltageMaxMv": 8400,
    "chargeCurrentMinMa": 500,
    "chargeCurrentMaxMa": 3000,
    "stableSampleCount": 3,
    "sampleIntervalMs": 500,
    "timeoutMs": 20000,
    "pmicBus": "i2c",
    "pmicCommand": "read_charge_status"
  }
}
```

### 15.2 PCBA 采样上报

PCBA 检测到充电器且完成连续稳定采样后，上报 `running` 并设置 `readyForHostDecision: true`：

```json
{
  "event": "test.report",
  "testId": "typec_fast_charge",
  "status": "running",
  "resultCode": 0,
  "message": "charge samples ready",
  "data": {
    "pmicCommunicationOk": true,
    "chargerConnected": true,
    "batterySimulationVoltageMv": 7400,
    "chargeVoltageMv": 8200,
    "chargeCurrentMa": 1850,
    "sampleIndex": 3,
    "readyForHostDecision": true
  }
}
```

### 15.3 上位机自动判定

上位机在 PMIC 通信正常、充电器已连接、充电电压和电流均落在下发范围内时发送：

```json
{
  "event": "test.decision",
  "source": "host_auto",
  "sessionId": "<sessionId>",
  "testId": "typec_fast_charge",
  "passed": true,
  "reason": "charge_values_in_range"
}
```

设备收到后保存简要结果并上报最终 `test.report / passed` 或 `failed`。失败原因可为 `charger_not_connected`、`pmic_communication_error`、`charge_voltage_out_of_range`、`charge_current_out_of_range` 或 `charge_timeout`；失败后停止后续测试并上报 `session.completed / failed`。

JK5506 的实际串口号、波特率和充/放电电流方向需在现场接线验证后配置，不能写死在 PCBA 协议中。

联调前可将上位机 `appsettings.json` 中的 `jk5506.enabled` 设为 `false`，以 Mock 方式跳过仪器控制；此时 TYPE-C 充电项仍按 Mock 的 PMIC 上报通过，不会中止后续项目。

## 16. `typec_camera` TYPE-C 相机自动测试协议

上位机只在 `session.start.tests` 中下发 `typec_camera` 及参数，不参与设备端拉流。PCBA 负责打开 TYPE-C 相机流、统计中断数并自动上报最终结果；上位机接收结果、失败时停止后续项目，并保存完整数据。

```json
{
  "id": "typec_camera",
  "parameters": {
    "streamProfile": "1080p30",
    "minInterruptCount": 200,
    "timeoutMs": 10000
  }
}
```

PCBA 最终上报：

```json
{
  "event": "test.report",
  "testId": "typec_camera",
  "status": "passed",
  "resultCode": 0,
  "message": "TYPE-C camera stream interrupt test passed",
  "data": {
    "streamProfile": "1080p30",
    "streaming": true,
    "interruptCount": 240,
    "minInterruptCount": 200,
    "frameRate": 30,
    "timeoutMs": 10000
  }
}
```

通过条件：`streaming: true` 且 `interruptCount >= minInterruptCount`。PCBA 拉流超时、打开流失败或中断数不足时上报 `failed`，在 `data.failureReason` 中分别使用 `stream_open_failed`、`stream_timeout`、`interrupt_count_below_threshold`，然后上报 `session.completed / failed`。设备本地仅保存通过/失败摘要，上位机保存完整 `data`。

## 17. `tf` TF 卡信息读取协议

上位机下发通用 TF 信息读取命令，PCBA 负责检测、挂载并读取卡信息后自动判定。上位机不直接访问 PCBA 上的文件系统，只显示并保存 PCBA 上报的结果。

```json
{
  "id": "tf",
  "parameters": {
    "command": "read_card_info",
    "mountTimeoutMs": 5000,
    "requireMounted": true
  }
}
```

PCBA 最终上报：

```json
{
  "event": "test.report",
  "testId": "tf",
  "status": "passed",
  "resultCode": 0,
  "message": "TF card info read passed",
  "data": {
    "command": "read_card_info",
    "cardDetected": true,
    "mounted": true,
    "cardName": "SD32G",
    "capacityMb": 32768,
    "fileSystem": "exfat",
    "cardInfoRead": true
  }
}
```

通过条件：检测到卡、成功挂载且读取卡信息成功。未插卡、挂载超时、挂载失败或信息读取失败时上报 `failed`，`data.failureReason` 使用 `card_not_detected`、`mount_timeout`、`mount_failed`、`card_info_read_failed`；设备停止后续测试并上报 `session.completed / failed`。设备本地仅保存通过/失败摘要，上位机保存完整 `data`。

## 18. `indicator_led` 与 `fan` 电压检测协议

两项共用 JX-TVM RS485 电压检测仪。上位机先发送 `test.control / set_output_level`，PCBA 仅按命令输出高、低电平；每次切换稳定后上报 `running` 和测量请求。上位机读取 JX-TVM 对应通道电压，按反相硬件规则自动判定，最后发送 `test.decision`；PCBA 保存摘要并上报最终结果。

JX-TVM 仅将 Windows 串口号配置在 `appsettings.json.jxTvm.portName`，例如 `COM7`。Modbus RTU 参数固定在上位机代码：从机地址 `1`、`9600/8N1`、读写超时 `1000ms`。实时通道 N 电压寄存器为 `1233 + (N - 1)`，单位 `0.01V`；上位机读取一个保持寄存器后乘以 `10` 换算为 mV。寄存器 `12-75` 用于阈值配置，不作为实时测量值读取。

```json
{
  "id": "indicator_led",
  "parameters": { "channel": 1, "highCommandExpectedVoltageMv": 0, "lowCommandExpectedVoltageMv": 3300, "toleranceMv": 150 }
}
```

上位机先发送：

```json
{ "event": "test.control", "testId": "indicator_led", "command": "set_output_level", "level": "high" }
```

PCBA 高电平阶段上报：

```json
{
  "event": "test.report",
  "testId": "indicator_led",
  "status": "running",
  "data": { "phase": "high", "measureRequest": true }
}
```

风扇使用相同协议，`testId` 改为 `fan`，并使用其配置的通道与目标电压。硬件反相规则固定为：上位机命令 `high` 时 JX-TVM 应读低电压（`highCommandExpectedVoltageMv`）；命令 `low` 时应读高电压（`lowCommandExpectedVoltageMv`）。两阶段读数均在目标值 `± toleranceMv` 内才通过，否则发送 `test.decision / passed: false / reason: voltage_out_of_range`。JX-TVM 通讯失败时发送 `reason: voltage_meter_communication_error`。设备端只保存通过/失败摘要，上位机保存高低电平两次读数与最终判定。

## 19. `battery_management` 电池放电测试协议

该项为电池放电测试。电池模拟器向 PCBA 提供设定电压，PCBA 进入规定负载状态后上报稳定采样；上位机依据放电电压和电流范围自动判定。Mock 阶段不连接任何真实仪器，直接使用模拟读数。

```json
{
  "id": "battery_management",
  "parameters": {
    "batterySimulationVoltageMv": 7400,
    "dischargeVoltageMinMv": 7000,
    "dischargeVoltageMaxMv": 7600,
    "dischargeCurrentMinMa": 100,
    "dischargeCurrentMaxMa": 1500,
    "stableSampleCount": 3,
    "sampleIntervalMs": 500,
    "timeoutMs": 15000
  }
}
```

PCBA/仪器采样稳定后上报：

```json
{
  "event": "test.report",
  "testId": "battery_management",
  "status": "running",
  "data": {
    "batterySimulationVoltageMv": 7400,
    "dischargeVoltageMv": 7380,
    "dischargeCurrentMa": 420,
    "sampleIndex": 3,
    "readyForHostDecision": true
  }
}
```

上位机按电压、电流上下限发送 `test.decision / source: host_auto`。通过时设备保存通过摘要并继续；超范围、采样超时或仪器通讯失败时发送失败判定并终止后续项目。JX-TVM 仅可作为电压来源；电流必须由 JK5506 或具备电流采样能力的仪器提供。上位机保存完整电压、电流、阈值和判定原因，设备仅保存通过/失败摘要。
