# 电压检测仪 32 通道设计

## 1. 文档目标

本文档定义上位机侧电压检测仪的接入方式、32 通道配置方式、阈值判定方式和数据输出方式。

## 2. 定位

电压检测仪属于上位机直连常驻模块，职责是持续采样和告警，不参与 PCBA 板内部测试执行。

职责包括：

- 采集各通道电压值
- 采集需要的电流或辅助值
- 根据阈值判断是否异常
- 输出给上位机测试流程
- 保存采样数据

## 3. 模块划分

- `VoltageMeterAdapter`
- `VoltageMonitorService`
- `VoltageRuleEngine`
- `VoltageSampleStore`

## 4. 通道模型

### 4.1 单通道配置

```text
VoltageChannelConfig
- ChannelNo
- ChannelName
- Enabled
- LowLimit
- HighLimit
- Unit
- SampleIntervalMs
- CompareTarget
- AlarmLevel
```

### 4.2 字段说明

- `ChannelNo`：通道编号，1 到 32
- `ChannelName`：通道名称
- `Enabled`：是否启用
- `LowLimit`：下限
- `HighLimit`：上限
- `Unit`：单位，通常为 `V`
- `SampleIntervalMs`：采样间隔
- `CompareTarget`：该通道的目标对象
- `AlarmLevel`：告警级别

## 5. 配置文件

建议使用 `yaml`。

```yaml
voltageChannels:
  - channelNo: 1
    channelName: VBAT_MAIN
    enabled: true
    lowLimit: 3.60
    highLimit: 4.30
    unit: V
    sampleIntervalMs: 500
    compareTarget: battery
    alarmLevel: warning
  - channelNo: 2
    channelName: VBUS_TYPEC
    enabled: true
    lowLimit: 4.80
    highLimit: 5.20
    unit: V
    sampleIntervalMs: 500
    compareTarget: typec
    alarmLevel: warning
```

## 6. 32 通道扩展原则

- 以配置驱动，不写死在代码里
- 通道数从 1 到 32 统一建模
- 某些通道可禁用
- 后续新增产品只改配置，不改主逻辑

## 7. 采样流程

1. 上位机初始化电压检测仪
2. 读取通道配置
3. 启动周期采样
4. 每次采样生成 `VoltageSample`
5. 写入缓存和数据库
6. 若超阈值则触发告警

## 8. 数据模型

### 8.1 VoltageSample

```text
VoltageSample
- SessionId
- ChannelNo
- ChannelName
- Voltage
- Current
- Status
- Timestamp
```

### 8.2 VoltageAlarm

```text
VoltageAlarm
- SessionId
- ChannelNo
- ChannelName
- AlarmLevel
- AlarmReason
- Voltage
- ThresholdLow
- ThresholdHigh
- Timestamp
```

## 9. 判定逻辑

### 9.1 正常

采样值在阈值区间内，状态为正常。

### 9.2 告警

以下任一条件满足则告警：

- 电压低于下限
- 电压高于上限
- 通道掉线
- 采样异常

### 9.3 结果输出

电压检测仪的结果不直接判定整机 PASS/FAIL，而是作为测试项辅助依据和异常依据。

## 10. 上位机集成

上位机在测试流程中应支持：

- 开始采样
- 停止采样
- 查询当前通道值
- 查询告警状态
- 保存采样历史

## 11. 文件与存储

建议保存两类数据：

- 实时采样缓存
- 历史采样文件

文件命名建议带 `sn` 和时间戳，便于追溯。

## 12. 和测试流程的关系

电压检测仪通常在以下阶段启用：

- 测试开始前预采样
- 测试过程中持续采样
- 测试结束后补采样

它的职责是为测试过程提供状态和阈值依据，不参与 PCBA 内部执行。
