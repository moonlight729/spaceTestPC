# PCBA 与整机测试架构概览

本文档用于快速确认当前 PCBA 测试、整机测试的整体架构、测试项覆盖和主要阈值。内容以当前代码和 `SpaceTestPC.App/appsettings.json` 为准，不展开底层实现细节。

## 1. 整体架构

上位机负责扫码、生成会话、下发测试计划、展示实时结果、执行部分仪器判定、保存 SQLite/CSV 记录，并在测试结束后把简要结果同步回板端。

板端 `spacetest3576` 常驻运行，监听 `19001` 端口，接收上位机 JSON Lines 协议请求，按测试计划顺序执行硬件测试项，并持续上报 `test.report`，最后上报 `session.completed`。

当前通信链路支持：

- `adbForward`：通过 ADB forward 连接板端 `19001`，兼容现有 Type-C 调试模式。
- `tcp`：PC 和板子接入同一路由器后，通过 IP 探测连接板端 `19001`。
- OTA：ADB 模式继续走 ADB；TCP 模式通过 SSH/SCP 上传并替换 `/vendor/originflow/bin/spacetest3576`。

当前配置中的默认测试模式是 `finished_product`。PCBA 与整机共用同一套协议和 runner，通过 `mode` 参数、测试项启停和个别测试行为区分。

## 2. 测试流程

内置测试顺序如下：

`board_state -> hdmi -> keys -> lcd -> ethernet -> wifi -> bluetooth -> fingerprint -> battery_management -> typec_fast_charge -> typec_camera -> tf -> usb2_3 -> pcba_test_points -> indicator_led -> fan -> otg -> reset_button`

上位机下发启用项后，板端按顺序执行。单项失败后不立即中断，会继续执行后续项；最终只要有一个计入结果的测试项失败，整轮结果应为 Fail。跳过项会记录为 `SKIPPED`，但不计入最终失败。

## 3. 测试项覆盖

| 测试项 | PCBA | 整机 | 当前机制 | 完善度 |
|---|---|---|---|---|
| `board_state` 板状态 | 是 | 是 | 读取板端状态、SN、历史摘要 | 基本完成 |
| `hdmi` | 是 | 是 | 人工观察，上位机 PASS/FAIL | 基本完成 |
| `keys` 五向键/Recovery | 是 | 是 | evdev 检测五向键；整机增加 Recovery ADC 检测 | 基本完成 |
| `lcd` | 是 | 是 | 人工观察，上位机 PASS/FAIL | 基本完成 |
| `ethernet` 网口 | 是 | 是 | 等待插网线，检查 link/IP/ping | 基本完成 |
| `wifi` | 是 | 是 | 板端 `nmcli` 扫描，上位机按 RSSI 自动判定 | 已压力验证，基本完成 |
| `bluetooth` | 是 | 是 | 上位机广播，板端 `bluetoothctl` 扫描 | 基本完成 |
| `fingerprint` 指纹 | 计划项 | 计划项 | 当前配置跳过或板端未真实实现 | 待接真实模组 |
| `battery_management` 板放电 | 是 | 是 | 板端关闭充电并等待上位机仪器判定 | 框架完成，依赖仪器阈值 |
| `typec_fast_charge` 板快充 | 是 | 是 | 板端使能充电并等待上位机仪器判定 | 框架完成，依赖仪器阈值 |
| `typec_camera` Type-C 相机 | 是 | 是 | 等待 `/dev/video*`，拉流/中断或 PWM 计数 | 基本完成，需实物覆盖验证 |
| `tf` TF 卡 | 是 | 是 | 检查设备、挂载、读写 | 基本完成 |
| `usb2_3` USB2/USB3 | 是 | 是 | 读取 USB 预检结果文件 | 机制完成，依赖预检文件生产 |
| `pcba_test_points` 32 路测试点 | 是 | 可选 | 读取 32 通道电压结果文件并按阈值判定 | 合约完成，真实采集后端待完善 |
| `indicator_led` RGB 指示灯 | 是 | 是 | 红/绿/蓝顺序点亮，人工判定；绿灯走 I2C | 已完成当前需求 |
| `fan` 风扇 | 是 | 是 | PWM 启动，读取 `tach_rpm` 自动判定 | 已压力验证，基本完成 |
| `otg` | 计划项 | 计划项 | 当前未在配置启用 | 待定义 |
| `reset_button` | 计划项 | 计划项 | 当前配置跳过 | 待定义 |

## 4. 当前关键阈值

| 项目 | 关键参数 | 当前值 |
|---|---|---|
| Wi-Fi | 目标 SSID | `test_router_001` |
| Wi-Fi | 最低 RSSI | `-75 dBm` |
| Wi-Fi | 最大重试次数 | `5` |
| Wi-Fi | 单次扫描超时 | `10000 ms` |
| Wi-Fi | 上位机判定超时 | `5000 ms` |
| 网口 | 网卡 | `end0` |
| 网口 | 路由器 IP | `192.168.31.1` |
| 网口 | ping 次数 | `4` |
| 网口 | 等待插线超时 | `30000 ms` |
| 蓝牙 | 广播名 | `yctc_bt_01` |
| 蓝牙 | 最低 RSSI | `-80 dBm` |
| 蓝牙 | 扫描窗口 | `10000 ms` |
| 按键 | 五向键超时 | `45000 ms` |
| Recovery | ADC 判定阈值 | `< 100` |
| Recovery | 稳定采样次数 | `3` |
| Recovery | 超时 | `10000 ms` |
| 快充 | 电压范围 | `7400-8400 mV` |
| 快充 | 电流范围 | `425-650 mA` |
| 快充 | 采样间隔 | `500 ms` |
| 放电 | 电压范围 | `7000-7600 mV` |
| 放电 | 电流范围 | `100-500 mA` |
| 放电 | 采样时长 | `10000 ms` |
| 放电 | 电流稳定容差 | `80 mA` |
| 相机 | 最小中断计数 | `200` |
| 相机 | 超时 | `10000 ms` |
| TF 卡 | 挂载超时 | `5000 ms` |
| USB2/USB3 | 预检要求 | USB2 `4` 次，USB3 `4` 次 |
| PCBA 测试点 | 通道数量 | `32` |
| PCBA 测试点 | 默认电压范围 | `0-5000 mV` |
| 指示灯 | RGB 每色点亮时间 | `2000 ms` |
| 指示灯 | 红切绿重叠时间 | `200 ms` |
| 指示灯 | I2C 超时/重试 | `3000 ms` / `100 ms` |
| 风扇 | PWM 路径 | `/sys/class/hwmon/hwmon12/pwm1` |
| 风扇 | tach 路径 | `/sys/class/hwmon/hwmon12/tach_rpm` |
| 风扇 | 启动/停止值 | `100` / `0` |
| 风扇 | 启动等待 | `3000 ms` |

## 5. PCBA 与整机差异

PCBA 模式更关注板级接口、电压测试点、USB 预检文件和基础硬件连通性。

整机模式更关注外设装配后的可见结果和人工确认，例如 HDMI、LCD、RGB 指示灯、风扇实际运行、Recovery 按键等。

当前代码里两种模式的主要差异不是协议不同，而是测试项启停、跳过策略和个别项目的执行方式不同。例如：

- `indicator_led` 在整机模式按红、绿、蓝顺序点亮并等待人工判定。
- `fan` 当前通过 PWM 启动并读取 `tach_rpm` 自动判定。
- `keys` 在整机模式包含 Recovery ADC 检测。
- `pcba_test_points` 更偏 PCBA 模式，目前依赖外部结果文件或后续真实采集后端。

## 6. 测试点完善度判断

当前主链路、协议、结果保存、Wi-Fi、风扇、LED、网口、蓝牙、TF、按键等基础项已经比较完整。

需要重点补齐或确认的测试点：

1. `pcba_test_points`：32 路电压测试点已有数据合约和判定逻辑，但真实采集后端仍是关键缺口。
2. `usb2_3`：判定依赖预检结果文件，需要确认板端 USB 预检流程能稳定生成对应文件。
3. `fingerprint`：当前未接真实指纹模组。
4. `otg`、`reset_button`：测试定义和判定方式还不完整。
5. `typec_fast_charge`、`battery_management`：流程已接入，但最终准确性依赖上位机外部仪器采样和阈值是否符合量产标准。
6. `typec_camera`：机制已具备，建议用真实相机覆盖插入等待、拉流、曝光/PWM 计数异常场景。

## 7. 建议

如果目标是判断测试点是否完善，优先闭环以下事项：

1. 明确 PCBA 32 路测试点每一路的名称、正常电压范围和真实采集来源。
2. 明确 USB2/USB3 预检流程是否纳入正式测试，以及结果文件由谁生成。
3. 确认指纹、OTG、Reset 是否是量产必测项；如果不是，应在配置里长期跳过并记录原因。
4. 对快充、放电的仪器采样阈值做一次量产标准确认。
5. 将 PCBA 和整机分别维护一份正式 `testModes` 配置，避免临时配置影响量产判断。
