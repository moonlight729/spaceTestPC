# 通信协议说明

## 1. 文档目标

本文档定义上位机与 PCBA 测试板之间的统一通信格式，目的是让两端能够并行开发，并保证测试会话、命令、结果和板状态一致。

配套文档：

- [上位机总体设计方案.md](C:/Users/31239/work/spaceTestPC/上位机总体设计方案.md)
- [PCBA测试板执行设计.md](C:/Users/31239/work/spaceTestPC/PCBA测试板执行设计.md)

## 2. 设计原则

- 命令统一
- 响应统一
- 状态统一
- 字段固定
- 版本可扩展
- 上位机和 PCBA 使用同一套 `sessionId` 和 `sn`

## 3. 基本对象

### 3.1 HostCommand

上位机下发给 PCBA 的命令对象。

```json
{
  "protocolVersion": "1.0",
  "requestId": "uuid",
  "sessionId": "uuid",
  "sn": "SN123456",
  "boardId": "PCB001",
  "commandGroup": "wifi",
  "command": "connect_and_ping",
  "parameters": {},
  "timestamp": "2026-07-13T16:00:00+08:00"
}
```

字段说明：

- `protocolVersion`：协议版本
- `requestId`：单次请求唯一标识
- `sessionId`：一次整板测试会话标识
- `sn`：当前待测板 SN
- `boardId`：PCBA 板唯一标识
- `commandGroup`：命令分组
- `command`：具体命令
- `parameters`：命令参数
- `timestamp`：发送时间

### 3.2 CommandResponse

PCBA 返回给上位机的结果对象。

```json
{
  "protocolVersion": "1.0",
  "requestId": "uuid",
  "sessionId": "uuid",
  "sn": "SN123456",
  "boardId": "PCB001",
  "resultCode": 0,
  "message": "ok",
  "data": {},
  "timestamp": "2026-07-13T16:00:01+08:00"
}
```

### 3.3 BoardState

PCBA 本地状态查询返回对象。

```json
{
  "boardId": "PCB001",
  "boardSn": "SN123456",
  "testMode": "ready",
  "currentState": "idle",
  "lastSessionId": "",
  "lastStartTime": "",
  "lastEndTime": "",
  "lastVerdict": "",
  "passCount": 0,
  "failCount": 0,
  "totalCount": 0,
  "version": 1
}
```

## 4. 协议流程

### 4.1 测试开始前

1. 上位机扫描 SN
2. 上位机创建 `sessionId`
3. 上位机发送 `sys.get_board_state`
4. PCBA 返回当前板状态
5. 若 `boardSn` 为空，上位机发送 `sys.write_sn` 写入扫码 SN，并重新读取板状态校验
6. 若 `boardSn` 与扫码 SN 一致，继续测试；若不一致，停止测试并报错，禁止覆盖

### 4.2 测试执行中

1. 上位机按测试流程下发命令
2. PCBA 执行命令并返回结果
3. 上位机记录结果到数据库和 CSV
4. PCBA 可按需更新本地状态文件

### 4.3 测试完成后

1. 上位机下发结束命令
2. PCBA 更新本地状态摘要
3. 上位机计算总记录和直通率
4. 上位机完成存档

## 5. 命令格式

### 5.1 系统类

- `sys.ping`
- `sys.get_info`
- `sys.get_board_state`
- `sys.write_sn`
- `sys.enter_test_mode`
- `sys.exit_test_mode`

### 5.2 蓝牙类

- `bt.scan_target_name`
- `bt.get_scan_result`

### 5.3 WiFi 类

- `wifi.connect_and_ping`

### 5.4 网线类

- `eth.connect_and_ping`

### 5.5 其他测试类

- `tf.*`
- `lcd.*`
- `fp.*`
- `key.*`
- `hdmi.*`
- `typec.*`
- `battery.*`
- `fan.*`
- `otg.*`
- `camera.*`

## 6. 参数规范

### 6.1 写入 SN

```json
{
  "commandGroup": "sys",
  "command": "write_sn",
  "sn": "SN123456",
  "boardId": "PCB001",
  "parameters": {
    "verifyReadBack": true
  }
}
```

该命令仅允许在设备 `boardSn` 为空时写入。设备已存在不同 SN 时必须返回非零 `resultCode`，不得覆盖；成功后上位机会再次调用 `sys.get_board_state`，确认 `boardSn` 与扫码 SN 完全一致。

### 6.2 蓝牙

```json
{
  "targetName": "NODE_A_01",
  "timeoutMs": 5000,
  "minRssi": -80
}
```

### 6.3 WiFi

```json
{
  "ssid": "FactoryAP",
  "password": "12345678",
  "targetIp": "192.168.1.1",
  "pingCount": 4,
  "timeoutMs": 10000
}
```

### 6.4 网线

```json
{
  "routerIp": "192.168.1.1",
  "targetIp": "192.168.1.1",
  "pingCount": 4,
  "timeoutMs": 10000
}
```

## 7. 响应规范

### 7.1 成功

- `resultCode = 0`
- `message = ok`
- `data` 返回结构化结果

### 7.2 失败

- `resultCode != 0`
- `message` 返回可读错误信息
- `data` 可附带原始错误原因

### 7.3 建议错误码

- `0`：成功
- `1000-1999`：参数错误
- `2000-2999`：通信错误
- `3000-3999`：执行异常
- `4000-4999`：测试失败

## 8. 兼容要求

- 协议版本固定写入 `protocolVersion`
- 上位机和 PCBA 必须共享 `sessionId`
- `sn` 和 `boardId` 必须在整轮测试内保持一致
- `requestId` 必须用于请求响应配对

## 9. 扩展建议

后续如需新增命令，优先遵循以下方式：

1. 不修改已有字段名
2. 只新增 `commandGroup` 或 `command`
3. 保持返回结构稳定
4. 保持错误码范围稳定
