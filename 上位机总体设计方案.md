# PCBA 测试上位机总体设计方案

## 1. 项目目标

本项目目标是构建一套可扩展的 PC 端上位机软件，用于产线或研发阶段对 PCBA 板及外接模块进行自动化测试、参数配置、结果展示、数据存档和协议下发。

当前阶段先完成一个最小闭环：

- 接入扫码枪
- 实时获取扫码结果
- 在界面展示扫码内容
- 将扫码结果纳入统一测试任务流

后续逐步扩展以下能力：

- USB 蓝牙模块参数设置
- 电压检测仪数据采集
- 通过 `adb forward` 与 PCBA 设备通信
- 主控端向被测设备发送测试协议
- 测试蓝牙、WiFi、网线、TF 卡、SPI LCD、SPI 指纹、按键、HDMI、Type-C 快充、电池管理、风扇、OTG、相机信号线等
- 将所有测试过程与结果写入 PC 数据库

## 2. 总体设计原则

- 分层清晰：UI、业务编排、设备通信、硬件测试隔离
- 模块可插拔：每类设备或测试项可独立接入
- 协议统一：所有测试动作统一抽象为命令、响应、事件、结果
- 可追溯：测试过程、原始数据、判定结果全部可落库
- 可并发：多个设备通信互不阻塞，复杂测试可独立线程运行
- 可扩展：新增硬件模块时不破坏现有架构

## 3. 推荐技术选型

如果以 Windows 上位机为主，推荐如下：

- 开发语言：`C# / .NET 8`
- UI 框架：`WPF` 或 `WinUI 3`
- 本地数据库：`SQLite`
- 串口通信：`.NET SerialPort` 或 `RJCP.SerialPortStream`
- USB/HID：根据设备协议选择 `HID`、`WinUSB` 或厂商 SDK
- ADB 通信：调用 `adb.exe` 并封装为适配器
- 日志：`Serilog`
- 依赖注入：`.NET Generic Host`
- 本地消息总线：进程内事件总线 + `Channel` / `BlockingCollection`

推荐 C# 的原因：

- Windows 设备接入成熟
- GUI 开发效率高
- 串口、USB、HID、进程调用、数据库生态完善
- 多线程和异步任务模型适合设备测试场景

如果你后面希望跨平台，可将通信层和业务层做成独立类库，UI 层后续替换为 Avalonia。

## 4. 上位机总体架构

上位机只保留自身职责，不展开 PCBA 测试板内部实现。PCBA 板侧详细设计独立见 [PCBA测试板执行设计.md](C:/Users/31239/work/spaceTestPC/PCBA测试板执行设计.md)。

### 4.1 分层

#### 1) UI 层

负责：

- 扫码显示
- 手动/自动启动测试
- 展示当前测试步骤
- 展示采样状态
- 展示测试明细和历史记录

#### 2) Test Orchestrator 层

负责：

- 接收 UI 指令
- 生成测试会话
- 向 PCBA 板下发命令
- 接收执行结果
- 接收电压检测仪和电池模拟器采样值
- 执行阈值判断
- 写数据库和日志

#### 3) Infrastructure 层

负责：

- ADB 转发
- 扫码枪接入
- 电压检测仪接入
- 电池模拟器接入
- SQLite
- 日志
- 配置管理

### 4.2 推荐调用关系

```text
上位机 UI
  -> TestExecutionController
    -> PcbaCommandClient
      -> PCBA 通信通道

TestExecutionController
  -> VoltageMonitorService
  -> BatterySimulatorService
  -> DatabaseRepository
  -> LogService
```

命名含义：

- `TestExecutionController`：上位机的测试执行入口，负责接收 UI 操作并驱动整体流程
- `PcbaCommandClient`：仅负责和 PCBA 通信，不负责业务判断
- `VoltageMonitorService`：负责电压检测仪的采样调度和阈值判断
- `BatterySimulatorService`：负责电池模拟器的采样、状态维护和数据输出
- `DatabaseRepository`：负责测试数据落库
- `LogService`：负责运行日志记录

### 4.3 边界

- 上位机负责发命令、收结果、展示、判定、存库
- 上位机不展开板级测试执行细节
- PCBA 板仅作为命令执行端和结果返回端对接
- 蓝牙模块初始化、电压检测仪、电池模拟器属于上位机直连资源

## 5. 模块划分建议

### 5.1 核心模块

- `MainHost`
- `TestExecutionController`
- `SessionService`
- `ResultJudgeService`
- `PcbaCommandClient`
- `DatabaseRepository`
- `LogService`
- `ConfigService`

### 5.2 上位机直连模块

- `ScannerAdapter`
- `AdbBridgeAdapter`
- `VoltageMeterAdapter`
- `BatterySimulatorAdapter`
- `BluetoothConfigAdapter`

### 5.3 采集与监控模块

- `ScannerService`
- `VoltageMonitorService`
- `BatterySimulatorService`
- `SessionContextService`

## 6. 核心对象模型

### 6.1 测试会话

```text
TestSession
- SessionId
- Sn
- ProductModel
- StartTime
- EndTime
- StationCode
- FinalVerdict
```

### 6.2 上位机下发命令

```text
HostCommand
- RequestId
- SessionId
- CommandGroup
- CommandCode
- Parameters
- Timestamp
```

### 6.3 执行结果

```text
CommandResponse
- RequestId
- SessionId
- ResultCode
- Message
- Data
- Timestamp
```

### 6.4 测试项结果

```text
TestItemResult
- ResultId
- SessionId
- TestCode
- TestName
- Verdict
- MeasuredValue
- LowerLimit
- UpperLimit
- Unit
- ErrorCode
- ErrorMessage
- StartTime
- EndTime
```

### 6.5 电压采样数据

```text
VoltageSample
- SessionId
- ChannelNo
- Voltage
- Current
- Status
- ThresholdLow
- ThresholdHigh
- Timestamp
```

## 7. 通信与控制设计

### 7.1 上位机与 PCBA 的通信定位

上位机和 PCBA 的对接关系定义为：

- 上位机发送测试命令
- PCBA 返回命令执行结果
- 上位机负责会话管理、界面展示和结果存档

### 7.2 通信方式

按你的场景，建议优先支持：

- `adb forward`
- 串口
- TCP 本地端口

其中 Android/Linux 设备优先用 `adb forward`。

### 7.3 上位机指令模型

建议统一为：

```json
{
  "requestId": "uuid",
  "sessionId": "uuid",
  "commandGroup": "wifi",
  "command": "connect_and_ping",
  "parameters": {
    "ssid": "FactoryAP",
    "password": "12345678",
    "targetIp": "192.168.1.1",
    "timeoutMs": 10000
  },
  "timestamp": "2026-07-13T16:00:00+08:00"
}
```

### 7.4 响应模型

```json
{
  "requestId": "uuid",
  "sessionId": "uuid",
  "resultCode": 0,
  "message": "ok",
  "data": {
    "pingOk": true,
    "avgDelayMs": 12
  },
  "timestamp": "2026-07-13T16:00:01+08:00"
}
```

### 7.5 结果码建议

- `0`：成功
- `1000-1999`：参数错误
- `2000-2999`：通信错误
- `3000-3999`：执行端错误
- `4000-4999`：测试不通过
- `5000-5999`：外部仪器异常

## 8. 对接命令范围

上位机文档只定义命令分组和对接格式，不定义 PCBA 板内部执行方法。具体执行设计见单独文档。

### 8.1 系统类

- `sys.ping`
- `sys.get_info`
- `sys.enter_test_mode`
- `sys.exit_test_mode`

### 8.2 测试类

- `bt.*`
- `wifi.*`
- `eth.*`
- `tf.*`
- `lcd.*`
- `fp.*`
- `key.*`
- `hdmi.*`
- `typec.*`
- `battery.*`
- `fan.*`
- `otg.*`
- `camera.*`

### 8.3 参数来源

- 蓝牙目标名称
- WiFi 参数
- 电压通道阈值
- 电池模拟器控制参数
- 产品型号对应测试流程

这些参数由上位机配置管理模块统一维护，再在执行时下发给 PCBA 板或本地仪器模块。

## 9. 扫码枪模块首期实现方案

扫码枪仍然是上位机侧的入口模块。

### 9.1 职责

- 读取条码或 SN
- 创建测试会话
- 自动触发测试流程

### 9.2 首期联动

流程建议：

1. 扫码枪读到 SN
2. 上位机创建 `SessionId`
3. 上位机向 PCBA 发送 `sys.enter_test_mode`
4. 上位机按测试流程逐项下发命令
5. 同时启动本地采样模块
6. 汇总并存库

## 10. ADB Forward 设计

如果 PCBA 侧提供本地测试服务，PC 端通过：

```text
adb forward tcp:19001 tcp:19001
```

访问 `127.0.0.1:19001` 即可。

`AdbBridgeAdapter` 只负责：

- 建立转发
- 检查在线状态
- 断开重连
- 清理端口

它不承担测试逻辑。

## 11. 电压检测仪设计

电压检测仪不是一次性读取，而是上位机侧常驻采集模块。

### 11.1 定位

负责：

- 持续读取多通道电压数据
- 未来扩展到 32 通道
- 为每个通道配置阈值
- 实时告警
- 为测试结果提供辅助判据

### 11.2 建议结构

- `VoltageMeterAdapter`：负责连接仪器和采集原始值
- `VoltageMonitorService`：负责定时采样、阈值检查、数据缓存
- `VoltageRuleEngine`：负责按产品型号加载 32 通道阈值

### 11.3 通道模型

```text
VoltageChannelConfig
- ChannelNo
- ChannelName
- Enabled
- LowLimit
- HighLimit
- Unit
- CompareTarget
```

`CompareTarget` 可表示该通道对应：

- 固定阈值判断
- 外部参考值判断
- 指定测试项判断

### 11.4 32 通道扩展建议

未来直接按配置驱动，不要把 32 个通道写死在代码里。

例如：

```yaml
voltageChannels:
  - channelNo: 1
    channelName: VBAT_MAIN
    lowLimit: 3.6
    highLimit: 4.3
  - channelNo: 2
    channelName: VBUS_TYPEC
    lowLimit: 4.8
    highLimit: 5.2
```

## 12. 电池模拟器设计

电池模拟器在上位机侧定位为常驻仪器模块。

### 12.1 职责

- 仪器连接和初始化
- 周期采样
- 当前状态展示
- 记录采样曲线
- 为测试过程提供参考数据

### 12.2 模块建议

- `BatterySimulatorAdapter`
- `BatterySimulatorService`
- `BatterySampleBuffer`

### 12.3 线程模型

- 采样线程
- 仪器控制线程
- 数据落库线程

## 13. 测试任务编排

建议上位机支持：

- 单项执行
- 整机顺序执行
- 指定测试集合执行

推荐主流程：

1. 扫码获取 SN
2. 创建测试会话
3. 读取 PCBA 本地测试状态
4. 连接 PCBA
5. 进入测试模式
6. 配置动态参数
7. 启动电压检测仪采样
8. 启动电池模拟器监控
9. 依次下发各项测试命令
10. 接收结果并判定
11. 汇总整机结果
12. 写数据库和 CSV

其中第 3 步是默认前置流程，用来保证上位机和 PCBA 当前测试状态一致。

## 15. 数据库设计建议

本地数据库仍推荐 `SQLite`。

存储策略建议分成两层：

- 数据库只存总记录、汇总结果和可检索索引
- CSV 文件存每个测试项的详细过程、测试时间和明细字段

### 15.1 主表

#### `test_session`

- `id`
- `session_id`
- `sn`
- `product_model`
- `start_time`
- `end_time`
- `final_verdict`
- `station_code`

#### `test_result`

- `id`
- `session_id`
- `test_code`
- `test_name`
- `verdict`
- `measured_value`
- `lower_limit`
- `upper_limit`
- `unit`
- `error_code`
- `error_message`
- `start_time`
- `end_time`

#### `test_summary`

- `id`
- `session_id`
- `sn`
- `board_id`
- `total_count`
- `pass_count`
- `fail_count`
- `pass_rate`
- `final_verdict`
- `generated_time`

#### `voltage_sample`

- `id`
- `session_id`
- `channel_no`
- `channel_name`
- `voltage`
- `current`
- `status`
- `timestamp`

#### `pcba_command_log`

- `id`
- `session_id`
- `request_id`
- `command_group`
- `command_code`
- `request_payload`
- `response_payload`
- `timestamp`

### 15.2 CSV 文件

CSV 文件用于保存更细的测试明细，建议按 SN 手动关联命名。

建议命名格式：

```text
SN_测试开始时间_工位号.csv
```

例如：

```text
SN123456_20260713_153000_ST01.csv
```

CSV 建议字段：

- `session_id`
- `sn`
- `board_id`
- `test_code`
- `test_name`
- `step_name`
- `start_time`
- `end_time`
- `duration_ms`
- `verdict`
- `measured_value`
- `unit`
- `remark`

### 15.3 直通率统计

整板测试完成后，系统根据本次会话的测试项结果计算板子直通率：

```text
pass_rate = pass_count / total_count
```

统计结果写入 `test_summary`，同时可在 UI 侧展示：

- 本次直通率
- 今日直通率
- 当前工位直通率

### 15.4 一致性要求

- 上位机读取的 `sn` 必须和 CSV 文件名一致
- 上位机数据库中的 `session_id` 必须和 PCBA 返回的 `sessionId` 一致
- PCBA 返回的板状态必须和上位机启动测试时读取到的状态一致
- 测试开始后，如 PCBA 状态变化，必须在结果中体现

## 16. UI 页面规划

### 16.1 首页

- 当前工位状态
- 当前 SN
- PCBA 在线状态
- 今日测试统计

### 16.2 测试执行页

- 扫码结果
- 测试步骤列表
- 当前执行项
- 测试通过/失败状态
- 实时日志

### 16.3 电压监控页

- 32 通道实时值
- 通道阈值状态
- 告警提示

### 16.4 电池模拟器页

- 电池模拟器状态
- 当前采样值
- 采样曲线
- 运行状态

### 16.5 配置页

- 产品型号
- 蓝牙目标名称规则
- WiFi 参数
- 网线测试参数
- 电压通道阈值
- CSV 命名规则
- 板状态文件映射规则

## 17. 并发与线程模型

上位机不需要复杂设备执行线程池，重点是常驻采集和通信稳定性。

### 17.1 建议线程

- UI 主线程
- PCBA 通信线程
- 电压检测仪采样线程
- 电池模拟器采样线程
- 日志/数据库写入线程

### 17.2 设计原则

- 所有 PCBA 测试执行请求串行化，避免状态混乱
- 电压检测仪持续采样，与测试流程并行
- 电池模拟器持续采样，与测试流程并行
- 采样数据异步写入，但归属当前测试会话

## 18. 配置设计

这个项目配置会很多，必须从第一天开始外置。

### 18.1 重点配置项

- PCBA 通信方式
- ADB 端口
- 蓝牙目标名称规则
- WiFi 测试参数
- 电压检测仪通道阈值
- 产品型号测试流程

### 18.2 配置示例

```yaml
productModel: PCBA_X1
bluetooth:
  targetName: NODE_A_01
  minRssi: -80
wifi:
  ssid: FactoryAP
  password: 12345678
  targetIp: 192.168.1.1
voltageChannels:
  - channelNo: 1
    channelName: VBAT_MAIN
    lowLimit: 3.6
    highLimit: 4.3
```

## 19. 异常处理

常见异常：

- PCBA 无响应
- ADB 断开
- 蓝牙目标参数未配置
- 电压检测仪掉线
- 电池模拟器掉线

处理原则：

- PCBA 命令必须带超时
- 仪器掉线立即告警
- 配置缺失不允许启动正式测试
- 采样异常应单独记录为明确失败原因

## 20. 分阶段实施建议

### 阶段 1

- 扫码枪
- 上位机主界面
- PCBA 指令通道
- 基础数据库

### 阶段 2

- 蓝牙目标名称参数化
- WiFi 连接并 ping 测试
- 基础结果存档

### 阶段 3

- 电压检测仪常驻采集
- 32 通道阈值配置
- 告警展示

### 阶段 4

- 电池模拟器状态采集
- 采样曲线存档
- 测试过程联动展示

### 阶段 5

- 其他功能测试项逐步接入
- 产线化报表
- MES 对接

## 21. 推荐目录结构

```text
spaceTestPC/
├─ docs/
│  ├─ 上位机总体设计方案.md
│  ├─ 通信协议说明.md
│  ├─ 电压检测仪32通道设计.md
│  ├─ PCBA测试板执行设计.md
│  ├─ 数据库设计.md
│  └─ 测试流程配置说明.md
├─ src/
│  ├─ UI/
│  ├─ Orchestrator/
│  ├─ Infrastructure/
│  ├─ Domain/
│  └─ Persistence/
├─ config/
│  ├─ appsettings.json
│  ├─ product-flows.yaml
│  ├─ voltage-channels.yaml
│  └─ station-config.yaml
└─ data/
```

## 22. 最终结论

这版方案的核心调整是：

- 上位机收口为测试编排、采集、展示、存库平台
- PCBA 板执行细节不再写入本文件
- 上位机与 PCBA 通过统一命令/响应模型对接
- 电压检测仪和电池模拟器保留为上位机直连常驻模块

这样两份文档的职责边界会更清晰。

## 23. 下一步建议

如果继续往下做，最合理的顺序是：

1. 先补 `通信协议说明.md`
2. 再补 `电压检测仪 32 通道配置设计`
3. 再补 `电池模拟器详细设计`
4. 最后开始搭上位机工程骨架
