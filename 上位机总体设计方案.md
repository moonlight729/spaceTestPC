# PCBA 测试上位机总体设计方案

## 1. 项目定位与当前状态

本项目是 Windows 上的 PCBA 测试上位机，用于按 SN 启动测试、编排测试项、显示结果、保存本地记录，并查询已完成的测试记录。

当前实现已具备：

- WPF 桌面程序（`.NET 10 / net10.0-windows`）
- SN 扫码/手工输入入口，以及 Mock 一键测试
- Mock 与 ADB 两种 PCBA 客户端适配器；当前流程默认使用 Mock
- 按配置启用测试项：板状态、HDMI、五键、LCD、Wi-Fi、蓝牙、指纹模组、板快充、Type-C 相机、TF 卡、指示灯、风扇、板放电
- JK5506 电池模拟器、JX-TVM 电压检测仪和蓝牙广播模块的串口服务封装
- SQLite 会话与测试项结果存档，以及按 SN 的 CSV 导出
- 测试序列自动跟随当前运行项；完成后可手动选择任一项查看结果
- 扫描已有 SN 时弹出历史记录窗口；查询页可查看最近 200 条记录并按 SN 或板号筛选

未实现或尚未接入生产的能力：正式 ADB/板端联调、32 通道持续采集、直通率统计、板端历史补同步、MES 对接、配置管理页面。

## 2. 已实现架构

```text
MainWindow (WPF)
  -> MainViewModel
    -> PcbaCommandClientFactory
       -> MockPcbaCommandClient / AdbPcbaCommandClient
    -> ScannerService
    -> SqliteDatabaseRepository
    -> Jk5506Service / JxTvmService / BluetoothBroadcasterService
    -> LogService / ConfigurationService
```

- `MainWindow`：展示测试页、查询页和历史记录弹窗。
- `MainViewModel`：维护测试会话、测试序列、人工判定、结果展示和数据保存。
- `IPcbaCommandClient`：统一抽象板端命令与会话事件；Mock 用于本地验证，ADB 用于后续真实板端对接。
- 仪器服务：仅在相应测试项中按需调用，不是常驻的后台 32 通道采集服务。
- `SqliteDatabaseRepository`：保存会话、结果、导出状态并补导出 CSV。

## 3. 测试流程

```text
扫描/输入 SN
  -> 查询本地最新记录
  -> 已存在：显示历史记录弹窗
  -> 不存在：创建 SessionId，进入板端测试模式
  -> 按测试计划接收 test.report 事件
  -> 更新测试序列、详情、人工判定与仪器测量
  -> 接收 session.completed
  -> 保存 SQLite、导出/补导出 CSV
```

整机完成以 `session.completed` 为准；最后一个测试项 PASS 本身不等同于会话完成。测试执行顺序由上位机生成的 `tests[]` 强约束，设备端必须按该顺序执行。单项失败后继续执行后续项，最终 `session.completed` 汇总整板结果：全部通过为 `passed`，任一项失败为 `failed`。

五键测试包含 `up`、`down`、`left`、`right`、`confirm`；同时兼容板端将确认键上报为 `ok`。

## 4. 界面设计

### 4.1 测试页

- 顶部：测试/查询切换、板号、连接状态、版本。
- 扫码区：输入 SN 后开始新测试或查询历史。
- 左侧 `TEST SEQUENCE`：显示测试状态；自动滚动只在新测试项进入运行状态且超出安全可视区时触发。用户点击任意项只切换右侧详情，不影响自动滚动逻辑。
- 右侧：当前/选中测试项的状态、消息、结果码、耗时、设备数据；按键项显示五个按键状态。
- 底部：最近运行日志与 `Run Mock` 按钮。

### 4.2 查询页与历史弹窗

- `QUERY RECORDS`：默认载入最近 200 条 SQLite 会话，可按 SN 或板号筛选。
- 扫描已有 SN：弹出 `Test Record`，显示 SN、整机结果、时间、中文测试项名称、每项结果和结果码。
- 查询弹窗使用中文业务名称，例如“板快充”“板放电”；界面菜单和技术字段维持英文。

## 5. 数据存储

SQLite 文件位于程序目录下：`data/box-test-records.db`。

当前表结构：

| 表 | 作用 |
|---|---|
| `test_sessions` | 会话 ID、SN、开始/结束时间、整机结果、板号 |
| `test_results` | 测试项 ID、状态、结果码、消息、JSON 数据 |
| `csv_exports` | 会话 CSV 是否已成功导出的标记 |

CSV 位于 `data/records/<SN>.csv`，同一 SN 的多次会话追加写入同一文件。数据库是完整记录来源；CSV 用于人工查看和离线导出。

当前数据库不包含测试次数、首次直通率、最终直通率或按板端记录 ID 的去重。这些属于后续产线汇总需求，实施前须单独定义统计口径与迁移方案。

## 6. 配置与设备

运行配置为程序目录中的 `appsettings.json`：

- `jk5506`：电池模拟器串口与通信参数。
- `jxTvm`：电压检测仪串口。
- `bluetoothBroadcaster`：蓝牙广播模块串口与广播名称。
- `testPlan.enabledTests` / `disabledTests`：启用的测试项。
- `testPlan.testParameters`：Wi-Fi、蓝牙、板快充、板放电和电压阈值等参数。
- `testPlan.mock`：Mock 延时与故障注入。

生产环境应完成 ADB、串口和仪器联调后再切换离开 Mock；不要将 Mock 结果作为生产数据。

## 7. 通信边界

上位机负责会话管理、参数下发、结果展示、部分仪器判定和本地存档；PCBA 负责板级测试执行与事件上报。统一数据对象包括 `HostCommand`、`CommandResponse`、`TestSessionEvent`、`BoardState`。

ADB 客户端为现有适配器，真实板端命令、端口和事件协议以 [开发对接说明.md](开发对接说明.md) 与 [通信协议说明.md](通信协议说明.md) 为准。

## 8. 验证与后续计划

当前单元测试覆盖 SQLite 会话保存与读取。发布前应执行：

```powershell
dotnet test SpaceTestPC.slnx
dotnet publish SpaceTestPC.App\SpaceTestPC.App.csproj -c Release -r win-x64 --self-contained true -o .\artifacts\publish\win-x64
```

后续优先级：

1. 完成 ADB/板端真实协议联调，并切换生产连接模式。
2. 完成 JK5506、JX-TVM 和蓝牙模块真实硬件验证。
3. 增加按 SN 的完整历史、返测次数和直通率统计。
4. 增加 32 通道电压采集、告警和趋势展示。
5. 评估 MES 接口及板端历史记录的增量同步。
## 2026-07-16 同步：新增测试项和状态

### 测试计划顺序

当前上位机固定顺序为：

`板状态 -> HDMI -> 按键 -> LCD -> 网口 -> WiFi -> 蓝牙 -> 指纹模组 -> 板快充 -> TYPE-C 相机 -> TF 卡 -> USB2.0&3.0 -> PCBA测试点 -> 指示灯板 -> 风扇 -> 板放电`

其中 `网口` 必须在 `WiFi` 前执行，因为 3576 端网口测试会关闭 Wi-Fi，并且 Wi-Fi 测试会重新打开 Wi-Fi。

### 新增中间状态：不测试

上位机支持 `skipped` 状态，用于当前环境无法测试但不希望影响最终结果的项目：

- 界面显示为“不测试”。
- 数据库保存 `SKIPPED`。
- 最终 PASS/FAIL 不统计该项目。
- 3576 返回码为 `2900`。

当前默认 skip 项：指示灯板、风扇、指纹模组、USB2.0&3.0、PCBA测试点。

### 新增测试项

- `网口`：提示插入网线；3576 关闭 Wi-Fi，检测 link/IP/ping；完成后提示拔掉网线。
- `USB2.0&3.0`：提示测试前已插入过 2.0 U盘和 3.0 U盘并生成汇总文件；3576 读取汇总文件。
- `PCBA测试点`：预留 32 通道电压采集协议；3576 当前读取汇总文件并返回每个点位电压和判定结果。
